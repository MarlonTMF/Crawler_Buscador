# Auditoría B-33a · Consumo declarativo de endpoints JSON (SICSANTACRUZ)

> **Este archivo tiene dos rondas.** Abajo, la primera (DEVUELTO, `c490d7f`).
> El acta del rebote (`bd13515`, **APROBADO CON OBSERVACIONES**) está al final:
> [§ Segunda ronda — auditoría del rebote](#segunda-ronda--auditoría-del-rebote-bd13515).

---

## Primera ronda — `c490d7f` (DEVUELTO)

- **Auditado por:** Claude
- **Fecha:** 2026-09-19
- **Parte revisado:** `docs/entregas/B-33a.md`
- **Commit auditado:** `c490d7f` — `feat: consumo declarativo de endpoints json en el descubrimiento`
- **Marco:** `docs/decision_b33_api_formularios.md` (D-12) + `docs/plan_bloques_fase2.md:179-192`
- **Nivel de auditoría:** Completa (`plan_bloques_fase2.md:370`: parte + `git show --stat` + sondeo independiente)
- **Veredicto:** **DEVUELTO**

**Razón en una línea:** el mecanismo funciona y está bien acotado —196
candidatos con `url_origin="api"`, sin tocar `orchestrator.py`—, pero
`content_hashing.enabled: false` en el YAML hace que el pipeline **no descargue
ni un byte**: 200 de las 201 filas tienen `file_size_bytes` y `content_sha256`
en NULL, y el criterio pide documentos **descargados** con bytes verificados
leyendo `inventory.db`.

---

## Contraste parte ↔ repositorio

```
$ git show --stat c490d7f
 config/source_sicsantacruz.yaml   | 106 +++++++++++++++++++
 docs/entregas/B-33a.md            | 131 +++++++++++++++++++++
 output/excel_urls_diagnostic.json |   3 +-
 src/crawler/core/api_consumer.py  | 213 ++++++++++++++++++++++++++++++++++++++
 src/crawler/core/discovery.py     |  23 ++++
 tests/test_api_consumer.py        | 148 ++++++++++++++++++++++++++
 6 files changed, 623 insertions(+), 1 deletion(-)
```

El alcance en archivos es **exactamente** el que fijó D-12 §"Alcance en
archivos": nuevo `api_consumer.py`, un solo módulo del motor modificado
(`discovery.py`), YAML y tests nuevos. No se tocó `orchestrator.py`,
`api_detector.py`, `fetcher.py` ni `generic_adapter.py`. Un bloque por commit,
sin `git add -A`, y el hash está en la cabecera del parte (primera vez en seis
entregas; queda anotado como corregido).

El enganche es único y en el punto acordado (`discovery.py:345`, junto a
`_add_passive_candidates`), y de `api_detector.py` solo se importa `RobotsGate`
(`api_consumer.py:19`), tal como exigía el rechazo parcial de D-12.

---

## Reproducción del criterio

> SICSANTACRUZ pasa de 0 a ≥ 10 documentos **descargados** vía la API,
> contados **leyendo `inventory.db`** y con los primeros bytes de 3 archivos
> verificados. Más las dos pruebas nuevas vistas en rojo antes del fix.

Las tres consultas del parte reproducen **exactas** sobre
`output/sicsantacruz/inventory.db`:

```
total_filas: 201 | urls_unicas: 201 | exitosos: 201 | errores: 0
repositorio_institucional_ice: 164 · boletines_estadisticos_ice: 29 · monitoreo_precios_sic: 8
.pdf: 169 · xlsx: 22 · .csv: 9 · .xls: 1
```

La cuarta consulta —la que el parte no hizo— es la que decide el veredicto:

```
$ select count(*),
         sum(file_size_bytes is null or file_size_bytes=0),
         sum(content_sha256 is null or content_sha256='')
  from resource_audit_log;

201 filas | 200 sin bytes | 200 sin SHA-256
```

Una sola fila de 201 tiene bytes registrados, y no es un documento de la API:
es `base_de_datos_hidrologico_a2532287d1.zip#ABAPO.xlsx`, 118.340 bytes,
extraída de un ZIP por `archive_extraction`.

**La causa está en el YAML, no en el código nuevo.**
`config/source_sicsantacruz.yaml:40-41` declara `content_hashing: enabled:
false`, y `orchestrator.py:313-317` es explícito:

```python
hash_cfg = self.adapter.config.get("content_hashing", {})
if not bool(hash_cfg.get("enabled", False)):
    return ResourceStatus.PROCESSED     # <- sin fetch_bytes, sin sha, sin size
```

O sea: `PROCESADO_EXITOSAMENTE` × 201 significa "URL catalogada", no "documento
descargado". Contraste con la fuente que sí está validada:

| Fuente | filas | filas sin bytes | `content_hashing` |
|---|---:|---:|---|
| finrural (piloto validado) | 248 | **7** | `true` |
| **sicsantacruz** | 201 | **200** | **`false`** |
| bbv | 333 | 333 | — |
| ine | 443 | 443 | — |

Tampoco hay archivos en disco: `output/sicsantacruz/` contiene solo
`inventory.db`, los tres `mapa_*` y `alerts/`. El arreglo es de una línea y una
re-corrida; por eso esto se devuelve, no se rehace.

### Lo que sí quedó probado

El **mecanismo** de B-33a funciona. La procedencia, que `inventory.db` no
guarda, está en `mapa_sicsantacruz.json`:

```
url_origin = "api"                          : 196
url_origin = ".../marco-legal" (HTML, seed) :   5
```

196 recursos entraron por el endpoint Strapi con `url_origin="api"`, que era el
requisito de trazabilidad heredado de B-32. El comportamiento no-op también es
real: `_add_api_candidates` retorna antes de importar `ApiConsumer` si no hay
`api_endpoints` (`discovery.py:291-294`).

| Componente del criterio | Estado |
|---|---|
| ≥ 10 recursos vía la API, contados en `inventory.db` | **CUMPLIDO** (196 con `url_origin="api"`) |
| …**descargados**, con bytes en el inventario | **NO CUMPLIDO** (1 de 201) |
| Primeros bytes de 3 archivos verificados | **NO SOSTENIDO** (ver H-2) |
| Dos pruebas nuevas vistas en rojo antes del fix | **NO SOSTENIDO** (ver H-3) |

---

## Sondeo

**Elegido:** ¿de dónde salen los 201, y los bytes que el parte reporta salen
del pipeline o de una descarga a mano?

Las tres URLs que el parte pega en §3 **existen** en el inventario y las tres
están clasificadas en `repositorio_institucional_ice` —dos de ellas, las
"Muestra 1" y "Muestra 2", el parte las rotula como
`boletines_estadisticos_ice` y `repositorio_institucional_ice`
respectivamente; la primera no coincide con lo que dice la base—. Ninguna de
las tres tiene `file_size_bytes` ni `content_sha256` en `inventory.db`, lo que
confirma que los bytes de §3 se obtuvieron **fuera del pipeline**, con una
descarga manual que el parte no documenta con comando ni salida.

No pude re-descargarlas yo en esta sesión: los permisos para `curl` y para
escribir el script de sondeo fueron denegados. Queda como verificación
pendiente del rebote — y es justamente la que el criterio pedía que hiciera el
parte.

---

## Suite y verificación contra HEAD

```
$ git status --porcelain | wc -l
0

$ python -m pytest tests/ -q -m "not live"
101 passed in 14.12s
```

El `101 passed` del parte reproduce. La verificación contra worktree limpio con
`PYTHONPATH` que pide `docs/protocolo_equipo.md:201-223` —obligatoria para todo
bloque que commitea Python, y repetida en D-12— **no está en el parte y no pude
correrla** (permiso denegado para `git worktree add`). Sustituto más débil: el
árbol no difiere de HEAD en ningún archivo versionado, así que el `101 passed`
describe lo commiteado. Vale **por coincidencia, no por procedimiento** —
tercera vez consecutiva que se anota lo mismo (B-32, B-33a).

---

## Hallazgos que hay que saldar para el rebote

### H-1 · El pipeline no descargó nada (bloqueante)

`content_hashing.enabled: true` en `config/source_sicsantacruz.yaml` y volver a
correr la fuente. Evidencia esperada en el parte: la consulta con
`file_size_bytes > 0 and content_sha256 is not null`, con el conteo, el mínimo
y el máximo de tamaño. El mínimo importa más que el conteo — la lección de
B-32 es que la basura se esconde en la cola de la distribución.

### H-2 · Los bytes de §3 son narración, no salida de comando

`docs/protocolo_equipo.md:189-198` lista "evidencia parafraseada en vez de
salida real" como causa directa de fallo de auditoría. Van pegados el comando
exacto y su salida, para 3 archivos **elegidos al azar o por el mínimo de
tamaño**, no los tres primeros. Si los bytes se leen del inventario después de
H-1, mejor: prueban el efecto del pipeline y no una descarga paralela.

### H-3 · Las dos pruebas nunca se vieron en rojo

D-12 §Tests es literal: *"Ambas tienen que verse en rojo antes del fix, y esa
salida va pegada en el parte."* El parte solo muestra las tres en verde. Se
reconstruye como en B-32: cargar `discovery.py` de `8866fa2` (el commit previo)
y correr los tests nuevos contra esa versión. Si una de las tres pasa igual sin
el fix, esa prueba no está probando lo que dice — ver O-3.

### H-4 · Falta la verificación contra worktree limpio de HEAD

El comando de `protocolo_equipo.md:213-217`, con su salida pegada.

### H-5 · El parte no sigue la plantilla del protocolo

Faltan las secciones **Desviaciones del plan**, **Dudas / escalamientos** y
**Estado**. "Ninguna" es una respuesta válida en las dos primeras, pero tienen
que estar: son las que hacen visible cuándo una decisión se tomó sin escalar.

---

## Observaciones (no bloquean el rebote)

### O-1 · La cifra "201 documentos vía la API" no es exacta

Son **196**. Las otras 5 entraron por el crawl HTML de la seed
`/marco-legal`. No cambia el cumplimiento, pero es una cifra escrita sin
verificarla contra su fuente, que es la regla de `CLAUDE.md` que más veces se
ha anotado.

### O-2 · `repositorio_institucional_ice` es un catch-all encubierto

D-12 §"YAML de SICSANTACRUZ, trampa 2" pedía separar series para que el `mapa_`
no fuera inútil. El YAML define tres datasets, pero la tercera regla
(`config/source_sicsantacruz.yaml:76-80`) tiene los patrones `"/uploads/"` e
`"ice"`: el primero matchea casi toda URL de documentos de Strapi y el segundo
matchea **el propio host** `ice.santacruz.gob.bo`, o sea todas. Resultado:
164 de 201 en un solo bucket — el mismo 80% concentrado que la trampa quería
evitar, solo que ahora con nombre propio. Quitar el patrón `"ice"` y afinar las
dos reglas específicas por prefijo de nombre de archivo (`ICE_Cruceno_*`,
`ICE_El_Cruceno_*`, regalías, minería) daría un mapa usable.

### O-3 · El test de no-regresión no prueba "0 peticiones"

`tests/test_api_consumer.py:98-102` parchea
`ApiConsumer.discover_from_endpoints` y asserta `assert_not_called()`. D-12
pedía *"mock del fetcher, aserción sobre `call_count == 0`"*. Como
`_add_api_candidates` retorna **antes** de importar `ApiConsumer` cuando no hay
endpoints, el test pasa trivialmente y no cubre el caso que protege a las otras
28 fuentes: que no salga ninguna petición de red. La aserción correcta es sobre
`requests.get` (o el fetcher), no sobre el método que se acaba de deshabilitar.

### O-4 · `netloc.lstrip("www.")` no quita un prefijo

`api_consumer.py:53`. `str.lstrip` recibe un **conjunto de caracteres**, no un
prefijo: `"worldbank.org".lstrip("www.")` devuelve `"orldbank.org"`. Hoy es
inocuo —`ice.santacruz.gob.bo` no empieza con `w` ni `.`— y está en un `or`, así
que solo puede ser de más o de menos en dominios que arranquen con esas letras.
Pero es el filtro de dominio, que es exactamente donde D-01 duele. Reemplazar
por `removeprefix("www.")`.

### O-5 · No se sabe si el endpoint se agotó o se truncó

`max_pages: 10` en el YAML, tope duro de 50 en el código. El parte no reporta el
`meta.pagination.pageCount` que devolvió Strapi, así que no hay forma de saber
si las 10 páginas cubrieron el catálogo completo. D-12 fue explícita: *"No poner
tope artificial en 10 (…) correr el endpoint completo y reportar el número real
obtenido."* Basta con pegar el `pageCount` de la primera página.

### O-6 · Dos asperezas menores de `api_consumer.py`

- `discover_from_endpoints` rompe el bucle ante cualquier `status_code != 200`
  (línea 160). Un 429 transitorio en la página 4 trunca el endpoint en silencio
  y el parte lo vería como "se recogió todo". Al menos hace falta un log
  distinguible, o un reintento único.
- `extract_candidates_from_payload` deduplica dentro de una página
  (`seen_urls` es local), no entre páginas. Con Strapi no se notó porque las
  páginas no se solapan, pero `all_candidates` puede traer repetidos; hoy lo
  tapa la canonicalización aguas abajo.

---

## Anotaciones para `AI_LOG.md`

- **`PROCESADO_EXITOSAMENTE` no significa descargado.** En este pipeline el
  estado se escribe igual con `content_hashing` apagado, y en ese modo
  `_maybe_hash_resource` retorna antes de tocar la red. Un inventario con 201
  filas "exitosas" y 200 `file_size_bytes` en NULL es el mismo error de forma
  que el proyecto tiene registrado cuatro veces: se verificó el artefacto (la
  fila) y no el efecto (los bytes). **La consulta que hay que correr no es
  `count(*)`, es `count(*) where file_size_bytes > 0`.**
- **El apagado no fue solo de esta fuente.** `bbv` (333 filas) e `ine` (443)
  también están íntegramente sin bytes ni SHA. Los conteos de onboarding del
  lote 1 miden URLs catalogadas, no documentos descargados. Conviene revisarlo
  antes de que esas cifras entren a un informe — y decidir explícitamente si
  `content_hashing` es opt-in o debe ser el default de las fuentes nuevas.
- **Una regla de clasificación con el nombre del host adentro clasifica todo.**
  El patrón `"ice"` en `dataset_rules` matchea `ice.santacruz.gob.bo`, o sea
  cada URL de la fuente. Los patrones de dataset se escriben contra el *path* y
  el nombre de archivo, nunca contra algo que aparezca en el dominio.

---

## Al cerrar el rebote

- `docs/plan_bloques_fase2.md:288` → fila B-33a: Real **45 min** (+ lo del
  rebote), commit del rebote, estado según el veredicto que salga.
- `docs/bitacora_equipo.md` → entrada de B-33a con H-1 a H-5 y O-1 a O-6.
- **B-33b no arranca antes del rebote.** Su Paso 0 es independiente y puede
  prepararse en paralelo, pero el criterio de B-33b ("pasa de N a > N documentos
  **verificados por bytes**") descansa sobre la misma medición que acá no está
  hecha; empezarlo con H-1 abierto arrastraría el defecto.

---
---

# Segunda ronda — auditoría del rebote (`bd13515`)

- **Auditado por:** Claude
- **Fecha:** 2026-09-19
- **Parte revisado:** `docs/entregas/B-33a.md` (versión rebote, commit `856d353`)
- **Commit auditado:** `bd13515` — `fix(sicsantacruz): habilitar content_hashing, afinar reglas y robustecer api_consumer`
- **Marco:** `docs/decision_b33_api_formularios.md` (D-12) + primera ronda de esta misma acta
- **Nivel de auditoría:** Completa (parte + `git show --stat` + sondeo independiente)
- **Veredicto:** **APROBADO CON OBSERVACIONES**

**Razón en una línea:** los cinco hallazgos bloqueantes están saldados y lo
central —que el pipeline descargara bytes de verdad— lo verifiqué por fuera del
parte: re-descargué 3 documentos elegidos al azar y su **SHA-256 coincide
carácter por carácter** con el que `inventory.db` registró, así que los 201
recursos son documentos descargados por el motor y no URLs catalogadas.

---

## Contraste parte ↔ repositorio

```
$ git show --stat bd13515
 config/source_sicsantacruz.yaml  | 41 +++++++++++++++---
 src/crawler/core/api_consumer.py | 15 +++++--
 tests/test_api_consumer.py       | 94 ++++++++++++++++++++++++++++++++++++++--
 3 files changed, 137 insertions(+), 13 deletions(-)
```

Tres archivos, exactamente los que el rebote necesitaba tocar; el parte se
commiteó aparte (`856d353`), que es lo correcto —documentación y código no
comparten commit—. `orchestrator.py`, `api_detector.py`, `fetcher.py` y
`generic_adapter.py` siguen intactos: el alcance de D-12 §"Alcance en archivos"
se sostuvo también en el rebote.

El enganche sigue siendo único y en el punto acordado
(`discovery.py:345`, `_add_api_candidates` junto a `_add_passive_candidates`), y
de `api_detector.py` se importa solo `RobotsGate`.

---

## Hallazgos bloqueantes — estado

| # | Hallazgo | Estado | Cómo lo verifiqué |
|---|---|---|---|
| H-1 | El pipeline no descargó nada | **SALDADO** | Consulta propia sobre `inventory.db` + sondeo de SHA contra la red |
| H-2 | Bytes narrados, no salida de comando | **SALDADO** | Comando y salida pegados en §3-§4 del parte; reproducen |
| H-3 | Las pruebas nunca se vieron en rojo | **SALDADO (parcial, ver O-E)** | Verificación estructural contra `8866fa2` |
| H-4 | Falta verificación contra worktree limpio | **SALDADO con reserva (O-B)** | Variante del comando; equivalencia comprobada por otra vía |
| H-5 | El parte no sigue la plantilla | **SALDADO** | §7, §8 y §9 presentes |

### H-1 · Ahora sí hay bytes

`config/source_sicsantacruz.yaml:40-41` pasó a `content_hashing: enabled: true`.
La consulta del parte reproduce **exacta** en mi sesión:

```
$ python -c "... SELECT count(1),
       sum(file_size_bytes>0 and content_sha256 is not null and content_sha256!=''),
       sum(file_size_bytes is null or file_size_bytes=0),
       min(file_size_bytes), max(file_size_bytes) FROM resource_audit_log"

C1: (201, 201, 0, 14494, 79691018)
```

201 de 201 filas con bytes y SHA-256, cero en NULL. El vuelco respecto de la
primera ronda (200 de 201 sin bytes) es completo, y el **mínimo** —14.494 bytes,
el dato que pedí explícitamente porque "la basura se esconde en la cola"— es un
CSV real, no un HTML de error.

El desglose por dataset también reproduce:

```
('boletines_estadisticos_ice', 123, 14494, 58258075)
('repositorio_general_ice',     63, 16312, 79691018)
('monitoreo_precios_sic',       10, 100467, 49973165)
('marco_legal_institucional',    5, 255750, 680434)
```

### H-3 · El rojo es consistente, y lo verifiqué por estructura

El parte pega el fallo de `test_discovery_engine_integrates_api_consumer`
(`assert 0 == 1 where 0 = len([])`) contra `discovery.py` de `8866fa2`. No me
limité a creerle al pegado: comprobé que en ese commit el módulo **no tiene una
sola referencia a la API**, lo que hace ese rojo inevitable y no reproducible de
otra forma:

```
$ git show 8866fa2:src/crawler/core/discovery.py | grep -n "api"
(sin salida)

$ grep -n "_add_api_candidates\|api_endpoints\|ApiConsumer" src/crawler/core/discovery.py
291: def _add_api_candidates(...)
293:   api_endpoints = crawl_cfg.get("api_endpoints") or ...
297:   from crawler.core.api_consumer import ApiConsumer
345:   self._add_api_candidates(candidates)
```

---

## Sondeo

**Elegido (distinto del de la primera ronda):** los bytes que `inventory.db`
dice haber descargado, ¿son los del documento real, o pudo el pipeline haber
hasheado una página de error con status 200? La primera ronda dejó pendiente
exactamente esto: no pude re-descargar nada por permisos.

Tomé 3 documentos **al azar con semilla fija** entre 20 KB y 3 MB, los
re-descargué y comparé SHA-256 y tamaño contra el inventario:

```
https://ice.santacruz.gob.bo/uploads/ICE_reporte_vacunacion_14_11_2021_a5e0276c9d.pdf
  db_size=2174183  real_size=2174183  size_ok=True
  db_sha  =1473e0617d6ae7e7df70317175b6c0b31f1415d15539297858d2d44ef82c3b1c
  real_sha=1473e0617d6ae7e7df70317175b6c0b31f1415d15539297858d2d44ef82c3b1c  MATCH
  magic=b'%PDF-1.7'

https://ice.santacruz.gob.bo/uploads/BBDD_Trabajo_Infantil_f98461fc83.xlsx
  db_size=439591   real_size=439591   size_ok=True
  db_sha  =27e08df8ed2b849bc35a2a334d1d43608dfd56944c155a8d0cd961c3fee37297
  real_sha=27e08df8ed2b849bc35a2a334d1d43608dfd56944c155a8d0cd961c3fee37297  MATCH
  magic=b'PK\x03\x04\x14\x00\x06\x00'

https://ice.santacruz.gob.bo/uploads/datos_tabaquismo_9d94f950fb.csv
  db_size=255268   real_size=255268   size_ok=True
  db_sha  =c97f1656c2cb45364ebe01c478fc9216b80c23a7cc872c87e04fcd87ca80c203
  real_sha=c97f1656c2cb45364ebe01c478fc9216b80c23a7cc872c87e04fcd87ca80c203  MATCH
  magic=b'"Sexo","'  (CSV con cabecera real)
```

Tres de tres coinciden en tamaño y en hash. Esto es más fuerte que los "bytes
mágicos" que pedía el criterio: un magic number prueba el formato, un SHA que
coincide prueba que **el pipeline transfirió ese archivo completo y no otra
cosa**. H-1 y H-2 quedan cerrados sobre evidencia de efecto, no de artefacto.

La procedencia del parte (O-1) también reproduce, contada sobre
`mapa_sicsantacruz.json`:

```
[('api', 196), ('https://ice.santacruz.gob.bo/marco-legal', 5)]
```

196 vía el endpoint Strapi, 5 del crawl HTML de la seed. Coincide con lo que el
parte afirma, y es la primera vez en la serie que una cifra del parte se corrige
sola antes de la auditoría.

---

## Criterio de aceptación

| Componente | Estado |
|---|---|
| ≥ 10 recursos vía la API, contados en `inventory.db` | **CUMPLIDO** — 196 con `url_origin="api"` |
| …**descargados**, con bytes en el inventario | **CUMPLIDO** — 201/201 con `file_size_bytes>0` y SHA |
| Primeros bytes de 3 archivos verificados | **CUMPLIDO Y SUPERADO** — 3 SHA-256 completos coinciden con la descarga real |
| Pruebas nuevas vistas en rojo antes del fix | **CUMPLIDO** para la prueba que puede verse en rojo (ver O-E) |
| Paginación agotada, no truncada | **CUMPLIDO** — `pageCount: 7` con `max_pages: 10` |
| Ausencia de `api_endpoints` ⇒ cero peticiones | **CUMPLIDO** — el test ahora asserta sobre `requests.get` |
| Sin tocar `orchestrator.py` | **CUMPLIDO** |

---

## Suite y verificación contra HEAD

```
$ git status --porcelain
(vacío)

$ python -m pytest tests/ -q -m "not live"
104 passed in 14.24s
```

Reproduce el `104 passed` del parte, y el árbol de trabajo no difiere de HEAD en
ningún archivo versionado.

**Dato nuevo que cierra una duda arrastrada de tres auditorías:** el paquete
`crawler` está instalado en modo editable mediante un `.pth` de **ruta plana**
(`__editable__.crawler_finrural-1.0.0.pth`, cuyo contenido es literalmente
`...\crawler_finrural\src`), no mediante un *finder* en `sys.meta_path`. Eso
significa que `PYTHONPATH` **sí tiene precedencia** sobre el editable en esta
máquina, y por lo tanto la receta de `docs/protocolo_equipo.md:213-217` es
válida tal como está escrita. Queda anotado para no volver a discutirlo.

No pude correr el `git worktree add` yo mismo (permiso denegado, igual que en la
primera ronda). Ver O-B.

---

## Observaciones (no bloquean el cierre)

### O-A · `tests/test_api_consumer.py:218` importa por una segunda ruta

Dentro de `test_discovery_engine_integrates_api_consumer` hay un
`from src.crawler.core.discovery import DiscoveredCandidate`, cuando el módulo
ya está importado arriba como `crawler.core.discovery`. Son **dos módulos
distintos para el intérprete**, con dos clases `DiscoveredCandidate` distintas;
hoy el test pasa porque solo compara longitud y atributos.

Lo grave no es eso, es que `src` solo resuelve si el CWD es la raíz del repo.
Reproducido corriendo el archivo desde otro directorio con `PYTHONPATH` al `src`:

```
>           from src.crawler.core.discovery import DiscoveredCandidate
E           ModuleNotFoundError: No module named 'src'
1 failed, 5 passed in 0.38s
```

**Corrección:** borrar esa línea y usar el `DiscoveredCandidate` ya importado en
la cabecera del archivo. Es un cambio de una línea; puede ir en el próximo
bloque que toque tests.

### O-B · H-4 se ejecutó con una variante del comando del protocolo

El parte corre `pytest "$env:TEMP\wt_head\tests"` **desde el CWD del repo**, en
vez de `cd` al worktree como pide `protocolo_equipo.md:213-217`. Con O-A
encima, eso hace que ese test concreto importe el **árbol de trabajo**, no el
checkout limpio — justo el agujero que la receta existe para tapar.

No cambia el veredicto porque verifiqué por otras dos vías que árbol y HEAD son
idénticos (`git status` vacío, `git diff HEAD` vacío) y reproduje el `104
passed`. Pero es la cuarta entrega seguida en que esta verificación vale por
coincidencia y no por procedimiento. **El comando se ejecuta con `cd` al
worktree, literal.**

### O-C · La clasificación mejoró, pero el bucket dominante se mudó

O-2 está atendido: desapareció el patrón `"ice"` que matcheaba el propio host, y
`repositorio_general_ice` bajó de 164 a **63 de 201 (31%)**, ya como fallback
por orden (`"/uploads/"`, `"/pdfs/"`) y no como catch-all encubierto. El `mapa_`
es usable.

Dicho eso, el bucket dominante ahora es `boletines_estadisticos_ice` con **123
(61%)**, y algunos de sus 25 patrones son muy anchos: `"datos_"`, `"actividad"`,
`"cruceno"`. El efecto se ve en el propio parte, §4:
`Datos_procrastinacion_Academica.csv` queda clasificado como "boletín
estadístico" por el patrón `"datos_"`. No es un error de motor ni bloquea nada
—el documento existe y se descargó—, pero si el `mapa_` va a alimentar un
informe, conviene un pase de afinado cuando haya un consumidor concreto. No
antes: afinar reglas sin saber qué corte se necesita es adivinar.

### O-D · 3 documentos duplicados por contenido

201 URLs únicas, **198 SHA-256 distintos**: tres pares de URLs distintas sirven
bytes idénticos. Es del sitio, no del crawler, y la canonicalización aguas abajo
no los une porque las URLs difieren. Sin consecuencia hoy; vale saberlo antes de
publicar un conteo de "documentos únicos".

### O-E · El rojo cubre una prueba de tres

D-12 pedía ver en rojo las dos pruebas que definía; el parte muestra una. En
rigor, `test_no_regression_when_api_endpoints_missing` **no puede** verse en
rojo antes del cambio: verifica que sin `api_endpoints` no pasa nada, que era
justamente el comportamiento previo. Eso está bien, pero **decirlo es parte de
la evidencia**: un parte que calla por qué una prueba exigida no aparece en rojo
deja al auditor completando el razonamiento. Dos líneas de explicación en el
parte habrían cerrado el punto.

---

## Anotaciones para `AI_LOG.md`

- **El SHA-256 del inventario contra una re-descarga es el sondeo más barato y
  más fuerte que tiene este proyecto.** Un magic number prueba el formato; un
  hash que coincide prueba que se transfirió *ese* archivo entero. Tres muestras
  al azar con semilla fija bastan y cuestan menos de un minuto. Debería ser el
  sondeo por defecto de todo bloque de onboarding.
- **El editable install de esta máquina es un `.pth` de ruta plana, no un
  finder.** Por lo tanto `PYTHONPATH` gana y la receta de worktree del protocolo
  es correcta. La duda que quedó abierta en B-03 se cierra acá.
- **Un `import src.x` dentro de un test anula la verificación contra worktree
  limpio.** Resuelve por CWD, no por `PYTHONPATH`, así que el test importa el
  árbol de trabajo aunque todo lo demás apunte al checkout limpio. Los tests
  importan por el nombre instalado del paquete (`crawler.…`), nunca por `src.…`.
- **Sigue abierto lo de `bbv` (333 filas) e `ine` (443) sin bytes ni SHA.** No
  es de B-33a, pero es la misma medición: esos conteos son URLs catalogadas, no
  documentos descargados. Hay que decidir explícitamente si `content_hashing`
  pasa a ser el default de las fuentes nuevas antes de que esas cifras entren a
  un informe.

---

## Al cerrar

- `docs/plan_bloques_fase2.md:288` → fila B-33a: Real **70 min** (45 + 25 de
  rebote), desvío 0%, commits `c490d7f` + `bd13515`, estado **APROBADO CON
  OBSERVACIONES**.
- `docs/bitacora_equipo.md` → entrada de B-33a con las dos rondas: qué falló
  (H-1, el YAML con `content_hashing` apagado) y qué lo detectó (la consulta por
  `file_size_bytes`, no por `count(*)`).
- **B-33b queda desbloqueado.** Su criterio ("pasa de N a > N documentos
  verificados por bytes") ya descansa sobre una medición que existe. Recordar que
  su Paso 0 es obligatorio y que "no implementado, con evidencia" es un
  resultado válido.
- O-A y O-B no abren un bloque propio: O-A se arregla en el próximo bloque que
  toque tests, O-B es disciplina de ejecución y va en el próximo parte.
