# Acta de auditoría — B-56 · Estado de vigencia y paso a histórico

- **Auditor:** Claude (Opus 5)
- **Ronda 1:** `c7b09b1` — **DEVUELTO** (H-1, H-2, H-3). Acta íntegra en §R1, abajo.
- **Ronda 2:** `455ee5b` — **APROBADO CON OBSERVACIONES**

---

# R2 · Re-auditoría de la subsanación

- **Fecha:** 2026-09-25
- **Parte evaluado:** `docs/entregas/B-56.md` (159 líneas, re-entrega R2)
- **Commit auditado:** `455ee5b` — `fix(B-56): subsanacion de carga de recuperaciones, estabilidad de fechas y evidencia cruda`
- **Criterio de referencia:** `docs/plan_bloques_fase4.md:299-302`
- **Veredicto:** **APROBADO CON OBSERVACIONES**

> Los tres hallazgos están subsanados y lo verifiqué por efecto, no por
> descripción: el `recovery_attempts_log` dejó de estar vacío en el artefacto
> commiteado (bcb/deuda_externa con sus 3 intentos reales de B-54), y una
> re-corrida del script mueve **1 línea** del JSON —el `timestamp` de cabecera—
> contra las 22 de la ronda anterior: las 21 `transition_date` quedan quietas y
> los 21 `previous_state` están poblados. El §2.3 ahora es la salida cruda del
> comando y sus cifras por portal reproducen.
>
> Lo que decide el veredicto y no el DEVUELTO es que la mitad del criterio que
> faltaba —«quede registrado qué se intentó»— hoy está operable, y que el
> hueco que queda está **escalado correctamente en vez de disimulado**: los
> artefactos de B-54/B-54b solo persisten `status: RECOVERED`, así que la rama
> `HISTORICO` sigue sin poder ejercerse con datos reales. Confirmé el dato
> (`Counter({'RECOVERED': 3})` en los dos JSON) — el parte dice la verdad sobre
> su propia limitación, que es exactamente lo que el acta R1 pedía como
> alternativa aceptable a sintetizar `success` a mano.
>
> Las observaciones son de dos clases: una cifra en prosa que su propia salida
> pegada desmiente (9/7 cuando son 8/8), y dos caminos de la persistencia de
> fechas que ningún test cubre y que el sondeo de esta vuelta rompió. Ninguna
> cambia un estado ni produce una identidad equivocada.

---

## R2 §1 · Los tres hallazgos, verificados por efecto

### H-1 · Carga de la escalera de B-54 — **SUBSANADO**

`load_recovery_logs()` (`scripts/gestionar_ciclo_vida.py:81-97`) lee `recoveries`
y deriva `success` de `status == "RECOVERED"`. Sobre el artefacto commiteado:

```
$ python -c "... json.load('docs/entregas/ciclo_vida_datasets.json') ..."
total 21
logs no vacios: 1
--- bcb deuda_externa VIGENTE
    {'rung': 2, 'name': 'plantilla_serie', 'period': '2025-S2', 'success': True, 'url': 'https://www.bcb.gob.bo/webdocs/informes_deudaexterna/DEPEX%20dic25.pdf', 'status': 'RECOVERED'}
    {'rung': 2, 'name': 'plantilla_serie', 'period': '2024-S2', 'success': True, 'url': 'https://www.bcb.gob.bo/webdocs/informes_deudaexterna/DEPEX%20dic24.pdf', 'status': 'RECOVERED'}
    {'rung': 2, 'name': 'plantilla_serie', 'period': '2024-S1', 'success': True, 'url': 'https://www.bcb.gob.bo/webdocs/informes_deudaexterna/DEPEX%20jun24.pdf', 'status': 'RECOVERED'}
```

En R1 esa cuenta era `0`. El test `test_lifecycle_load_recovery_logs_real_json`
carga los JSON reales —no un mock—, que es lo que impide que el defecto vuelva.

**Escalamiento verificado, no aceptado de palabra.** El parte afirma que
B-54/B-54b no persisten intentos fallidos. Lo comprobé:

```
$ python -c "... Counter(r['status'] for r in d['recoveries']) ..."
docs/entregas/recuperaciones_b54.json  claves: ['timestamp','elapsed_seconds','total_recovered','recoveries']
  status: Counter({'RECOVERED': 3})   rungs: Counter({2: 3})
docs/entregas/recuperaciones_b54b.json claves: [... 'recoveries']
  status: Counter({'RECOVERED': 3})   rungs: Counter({2: 3})
```

Es cierto. La consecuencia queda registrada como O-6: `HISTORICO` es hoy una
rama probada solo en test. La dirección del error sigue siendo la segura
(archiva de menos), y ahora está dicha en voz alta en el parte en vez de
quedar escondida detrás de un `0`.

### H-2 · Estabilidad de `transition_date` — **SUBSANADO**

```
$ python scripts/gestionar_ciclo_vida.py
$ git diff --numstat docs/entregas/ciclo_vida_datasets.json
1       1       docs/entregas/ciclo_vida_datasets.json
$ git diff -U0 ... | grep "^[-+]" | grep -v "^[-+][-+][-+]" | grep -v '"timestamp"'
(sin salida)
```

R1: 22 líneas, todas fechas. R2: 1 línea, y es el `timestamp` de cabecera del
reporte —que **debe** moverse, porque es la marca de la corrida—. Las 21
`transition_date` quedan intactas. `previous_state` poblado en los 21 registros
(`previous_state None: []`).

### H-3 · Evidencia cruda y cifras por portal — **SUBSANADO**

El §2.3 es ahora la salida literal del script, incluidos los encabezados y la
línea de resumen. Contrasté las cuatro cifras corregidas contra mi propia
corrida: BCB 5, INE 8 (7 VIGENTE / 1 ATRASADO), ASFI 5 (4/1),
`balance_general` con atraso 8 y tolerancia 2, totales 21/16/2/3/0. Las cinco
reproducen.

---

## R2 §2 · Lo demás que verifiqué

| Qué | Cómo | Resultado |
|---|---|---|
| Archivos tocados vs. declarados | `git show --stat 455ee5b` | 6 archivos, 649 inserciones / 141 borrados. Sin `git add -A`, sin archivos de otro bloque (ver O-7 sobre el acta) |
| Suite completa | `python -m pytest tests/ -q -m "not live"` | `191 passed in 18.68s` — reproduce la cifra del parte |
| Código auditado ≡ HEAD | `git diff --stat HEAD -- src scripts tests` | vacío: lo que corrí es lo commiteado (ver O-3) |
| O-2 · cotejo exacto D-18 | `lifecycle.py:123-128` + `test_lifecycle_d18_exact_matching` | Igualdad exacta de `(portal, dataset_id)`. El test discrimina: propuesta `ACEPTADA` para `cuentas_nacionales_pib_trimestral` no migra `cuentas_nacionales_pib` |
| O-3 · sin periodicidad | `lifecycle.py:148-163` + salida real | `VIG_SIN_PERIODICIDAD` separa los dos sentidos de `VIGENTE`. Reparto real: 8 `VIG_AL_DIA` + 8 `VIG_SIN_PERIODICIDAD` (ver O-1) |
| O-1 · descripción del modelo | `lifecycle.py:48-77` | El parte ahora dice `@dataclass` stdlib mutable con `to_dict()`. Correcto |
| Corrida en rojo del §2.1 | lectura del traceback | Legítima y de una clase distinta a la de R1 (`NameError: name 'Tuple' is not defined`, no un módulo ausente). Cumple D-06 |

---

## R2 §3 · Sondeo

Construido contra el código de **esta** vuelta, no contra los hallazgos de la
anterior (regla de E-46). Elegí el mecanismo nuevo del bloque: la persistencia
de fechas entre corridas.

```
$ python -c "...dos casos contra lifecycle.py y load_previous_lifecycle..."
WARNING:scripts.gestionar_ciclo_vida:Error cargando ciclo de vida previo desde ...\prev.json: None
S1 regla VIG_AL_DIA -> VIG_SIN_PERIODICIDAD | fecha conservada: True
S2 registros recuperados con 1 fila corrupta: 0 (1 = degrada bien, 0 = se pierden todos)
```

Ambos casos salen por el lado no deseado; los dejo como O-2 y O-4. Ninguno se
activa con los datos de hoy, y por eso no devuelven el bloque.

---

## R2 §4 · Observaciones

**O-1 · Una cifra del parte que su propia salida desmiente.** El §O-3 del parte
dice «distinguiendo explícitamente los **9** datasets efectivamente al día de
los **7** donde no se mide periodicidad». El reparto real, contado sobre la
salida pegada doce líneas más abajo en el mismo documento, es **8 y 8**:

```
$ python -c "... Counter(x['transition_rule'] for x in ds) ..."
Counter({'VIG_SIN_PERIODICIDAD': 8, 'VIG_AL_DIA': 8, 'MIG_CATALOGO_HISTORICO': 3, 'ATR_BUSQUEDA_PENDIENTE': 2})
```

Es la misma miscuenta que cometí yo en el O-3 de R1 —enumeré ocho nombres y
escribí «7 de los 16»—, así que la heredó de mi acta, no la inventó. La anoto
igual porque la regla no cambia por quién la rompió: **ninguna cifra se escribe
sin contarla contra su fuente**, y una cifra en prosa contradicha por la salida
pegada al lado es justo el caso que el protocolo lista como causa de fallo. No
devuelve el bloque porque no está en la evidencia del criterio y porque la
salida correcta está ahí para desmentirla. Corregir a 8/8 en el parte.

**O-2 · `transition_date` se conserva cuando cambia la regla, no solo cuando
cambia el estado.** `_resolve_transition_date` (`lifecycle.py:112-115`) compara
únicamente `previous_record.state == target_st`. El caso S1 del sondeo: un
dataset que pasa de `VIG_AL_DIA` a `VIG_SIN_PERIODICIDAD` —porque el detector
dejó de reconocerle períodos— mantiene `VIGENTE` y por lo tanto **conserva la
fecha vieja**, aunque la regla de transición sí cambió. El plan pide «la regla
de transición y la fecha en que cambió»; con esto, la fecha responde por el
estado pero no por la regla. Comparar la tupla `(state, transition_rule)`.

**O-3 · La verificación contra HEAD limpio sigue sin hacerse como la pide el
protocolo.** El §O-4 del parte dice «verificación ejecutada en árbol de trabajo
limpio». Eso es precisamente lo que el protocolo declara insuficiente: el
paquete está instalado en editable, así que cualquier pytest en esta máquina
importa el árbol de trabajo. El procedimiento es `git worktree add --detach` +
`PYTHONPATH`. Lo intenté y el entorno de esta sesión no me autorizó a ejecutar
en el worktree; sustituí por `git diff --stat HEAD -- src scripts tests`, que
salió **vacío**, de modo que el código que corrí es byte a byte el de `455ee5b`
y el `191 passed` es válido sobre lo commiteado. Vale para esta vuelta, no
reemplaza el procedimiento en la siguiente.

**O-4 · Una fila corrupta en el reporte previo descarta las 21.** El `except` de
`load_previous_lifecycle` (`gestionar_ciclo_vida.py:131-133`) envuelve el bucle
entero y devuelve `{}`. Caso S2 del sondeo: un archivo con un registro bueno y
uno con `state: null` recupera **0** registros. La corrida siguiente trata los
21 datasets como primera evaluación y reescribe las 21 `transition_date` —el
mismo síntoma de H-2, reintroducido por un camino distinto—. Emite un `WARNING`,
así que no es del todo silencioso, pero el efecto visible es un reporte que se
ve normal con todas las fechas movidas. Mover el `try` adentro del bucle y
saltar la fila mala.

**O-5 · `load_previous_lifecycle` no tiene test.** El test de H-2
(`test_lifecycle_transition_date_stable_across_runs`) ejercita el manager
pasándole el `previous_record` en memoria; el ida y vuelta por disco —que es
donde vive O-4— no lo cubre ninguno. La estabilidad real la comprobé yo
re-corriendo el script, y esa comprobación no queda en la suite. Un test que
escriba el JSON, lo relea y afirme que la fecha sobrevive.

**O-6 · `HISTORICO` sigue sin ejercerse con datos reales.** Consecuencia del
escalamiento verificado en H-1: mientras `recovery_ladder` no persista los
intentos fallidos de los escalones 1..4, ningún dataset puede alcanzar
`ladder_exhausted`. La invariante que da nombre al bloque está probada
—`test_lifecycle_rejects_automatic_historico_by_silence` sigue siendo un control
negativo legítimo— pero la capacidad completa no es operable de punta a punta.
**Esto es entrada de B-57 o de un bloque propio, no un pendiente suelto**: la
decisión es si la escalera emite un registro de fallo por escalón probado.

**O-7 · El commit mezcla el acta de R1.** `455ee5b` incluye
`docs/auditorias/B-56.md` (221 líneas), que es artefacto del auditor, junto con
la subsanación. No es `git add -A` y el contenido es correcto, pero el acta
debería haber entrado en su propio commit para que el historial muestre dónde
terminó la auditoría y dónde empezó la corrección.

**O-8 · Estado del árbol.** Mi re-corrida del script dejó
`docs/entregas/ciclo_vida_datasets.json` con el `timestamp` de cabecera
modificado (1 línea). El comando de restauración requería aprobación que no se
otorgó; descartar con
`git checkout HEAD -- docs/entregas/ciclo_vida_datasets.json` antes de tomar el
bloque siguiente.

---

## R2 §5 · Qué se lleva cada quien

Para Antigravity, sin reabrir el bloque:

1. **O-1** — corregir 9/7 → 8/8 en el §O-3 del parte.
2. **O-2** — `_resolve_transition_date` sobre `(state, transition_rule)`.
3. **O-4** — `try` adentro del bucle de `load_previous_lifecycle`.
4. **O-5** — test de ida y vuelta por disco de la estabilidad de fechas.

Para Claude: entrada `E-47` escrita en `AI_LOG.md`. La fila de seguimiento de
`docs/plan_bloques_fase4.md:364` queda **sin actualizar** —la escritura no fue
autorizada en esta sesión—; el valor a poner es
`| B-56 Ciclo de vida e histórico | Antigravity | 80 min | 65 min | ✅ Aprob. c/obs (`455ee5b`, R2) |`.

**O-6** es una decisión, no una corrección: se resuelve al abrir B-57 o un
bloque propio para la persistencia de fallos de la escalera.

---
---

# R1 · Acta de la primera entrega (DEVUELTO)

- **Parte evaluado:** `docs/entregas/B-56.md` (125 líneas)
- **Commit auditado:** `c7b09b1` — `feat: ciclo de vida de datasets y archivado justificado`
- **Veredicto:** **DEVUELTO**

> El módulo es sólido en su núcleo y la invariante que da nombre al bloque —nada
> pasa a `HISTORICO` por silencio— está implementada y probada con un control
> negativo legítimo. Lo devuelvo por la otra mitad del criterio, que es la que
> no se sostiene: **«quede registrado qué se intentó»**. En el artefacto real los
> 21 registros tienen `recovery_attempts_log` vacío, porque
> `load_recovery_logs()` lee una clave que no existe en los JSON de B-54
> (`recovered_periods`; la clave real es `recoveries`) y, además, marca todo
> intento como `success: True`. Con cualquiera de los dos defectos por separado
> la rama `HISTORICO` es inalcanzable desde datos reales: el `0` de la columna
> HISTORICO del parte no prueba la invariante, la esconde.
>
> Y **«la fecha en que cambió» no es la fecha en que cambió**: no hay estado
> previo persistido, así que cada corrida reescribe `transition_date` de los 21
> datasets con `now`. Lo verifiqué re-corriendo el script sin tocar nada: los 21
> estados salen idénticos y las 21 fechas se movieron.
>
> Tercer motivo, de disciplina de evidencia: la tabla del §2.3 del parte no es
> la salida del comando y sus cifras por portal no reproducen (INE 10 vs 8 real,
> ASFI 3 vs 5 real, `balance_general` «2 períodos» vs 8 real). Los totales sí
> reproducen exactamente.

## R1 §1 · Lo que sí verifiqué y está bien

| Qué | Cómo lo verifiqué | Resultado |
|---|---|---|
| Archivos tocados = archivos declarados | `git show --stat c7b09b1` | Coincide: 5 archivos, 1164 inserciones, 0 borrados. Ningún `git add -A`, ningún archivo de otro bloque mezclado |
| Suite del proyecto | `python -m pytest tests/ -q -m "not live"` | `187 passed in 19.03s` — reproduce la cifra del parte |
| Reproducibilidad del reporte | `python scripts/gestionar_ciclo_vida.py` + `git diff docs/entregas/ciclo_vida_datasets.json` | 22 líneas cambiadas, **todas** `transition_date`/`timestamp`. Estados, reglas, atrasos y justificaciones: idénticos byte a byte |
| Totales del §2.3 | salida real del script | `{'VIGENTE': 16, 'ATRASADO': 2, 'MIGRADO': 3, 'HISTORICO': 0} (Total: 21)` — reproduce |
| Control negativo de la invariante | lectura de `tests/test_dataset_lifecycle.py:75-103` | `test_lifecycle_rejects_automatic_historico_by_silence` es un control negativo real: 9 períodos de atraso + solo el escalón 1 intentado y fallido → `ATRASADO`. Discrimina |
| Compuerta D-18 §6 | `src/crawler/core/lifecycle.py:108-131` + test `:146-192` | Solo `status == "ACEPTADA"` migra; `PROPUESTA` no. Consistente con D-18 §6 |
| Corrida en rojo del §2.1 | lectura del traceback | Legítima: `ModuleNotFoundError` por módulo inexistente, con los 7 tests ya escritos. Cumple D-06 |

**Sobre la verificación contra HEAD limpio** (protocolo §«Verificación contra
HEAD limpio»): el parte no la incluye y el bloque commitea Python, así que
correspondía. La corrí yo. En este caso el árbol estaba limpio al empezar la
auditoría (`git status --porcelain` vacío), de modo que árbol de trabajo ≡ HEAD
y el `187 passed` es válido sobre lo commiteado. Queda como observación de
forma (O-4), no como motivo de devolución.

## R1 §2 · Hallazgos que motivan la devolución

### H-1 · El registro de intentos de recuperación nunca se carga: la escalera de B-54 está desconectada

`scripts/gestionar_ciclo_vida.py:81` itera `data.get("recovered_periods", [])`.
Esa clave no existe en ninguno de los dos artefactos de B-54:

```
$ python -c "...json.load(...)"
docs/entregas/recuperaciones_b54.json  total_recovered= 3 len(recoveries)= 3 recovered_periods? False
docs/entregas/recuperaciones_b54b.json total_recovered= 3 len(recoveries)= 3 recovered_periods? False
```

Consecuencia medida sobre el artefacto commiteado:

```
$ python -c "... sum(1 for x in r['datasets'] if x['recovery_attempts_log'])"
logs no vacios: 0
```

**Los 21 registros tienen el log de intentos vacío.** El `try/except` de
`:90-91` no salta porque el JSON se parsea sin error: es exactamente la clase de
fallo que `CLAUDE.md` describe —no produce ningún error visible y devuelve un
resultado sin verificar—.

Hay un **segundo defecto independiente** en la misma función: `:87` escribe
`"success": True` fijo para todo intento. La invariante de
`lifecycle.py:158-165` exige `all(not att.get("success"))`, así que aun
arreglando la clave, ningún dataset podría nunca cumplir `ladder_exhausted`.
Con los dos defectos, **la rama `HISTORICO` es código muerto desde datos
reales**: el `HISTORICO: 0` del parte no es el resultado de una evaluación que
podría haber dado otro número.

Esto rompe la mitad del criterio del plan que dice «y quede registrado qué se
intentó». Hoy no queda registrado nada. La dirección del error es la segura —no
archiva de más, archiva de menos—, y por eso el bloque no está mal concebido;
pero la capacidad que anuncia el mensaje de commit («archivado justificado») no
es operable de punta a punta.

**Corrección pedida:** leer `recoveries`, derivar `success` del campo `status`
real del registro (`RECOVERED` → éxito; los fallos hay que emitirlos desde
`recovery_ladder.py` si hoy no se persisten — si no se persisten, decilo en el
parte en vez de sintetizarlos), y agregar un test que cargue los JSON reales de
B-54 y afirme que al menos un dataset queda con `recovery_attempts_log` no
vacío. Sin ese test el defecto vuelve: los 7 tests actuales pasan con la
función completamente rota, porque ninguno toca el script.

### H-2 · `transition_date` es la fecha de la corrida, no la fecha del cambio de estado

El plan pide «la regla de transición **y la fecha en que cambió**»
(`plan_bloques_fase4.md:295-296`). `lifecycle.py:100` fija
`now_iso = datetime.now(...)` y las cinco ramas lo escriben sin excepción; el
script nunca lee el reporte anterior y pasa `previous_state=None` en todas las
llamadas (`gestionar_ciclo_vida.py:186-192`). Verificado:

```
$ python scripts/gestionar_ciclo_vida.py
$ git diff --numstat docs/entregas/ciclo_vida_datasets.json
22      22      docs/entregas/ciclo_vida_datasets.json
# y el diff filtrado sin transition_date/timestamp está vacío:
$ git diff ... | grep "^[-+]" | grep -v "transition_date\|timestamp"
(sin coincidencias más allá de las cabeceras del diff)
```

Los 21 estados son idénticos y las 21 fechas cambiaron. Un campo que se mueve
cuando nada se movió no sirve para auditar cuándo algo se movió, que es
literalmente para lo que el plan lo pide. `previous_state` está en el modelo,
en los tests y en el JSON, pero en producción vale `None` en los 21 registros.

**Corrección pedida:** cargar el reporte anterior (si existe), y conservar
`transition_date` + poblar `previous_state` cuando el estado no cambió respecto
de la corrida previa. Con un test que corra dos veces y afirme que la fecha no
se movió en la segunda.

### H-3 · La evidencia del §2.3 no es la salida del comando y sus cifras por portal no reproducen

El protocolo es explícito: «se pega la salida real del comando. Una descripción
de la salida no es evidencia». El §2.3 presenta una tabla markdown construida a
mano. Al correr el comando, tres cifras no coinciden:

| Cifra del parte | Real (salida del script) |
|---|---|
| INE: 10 datasets (9 VIGENTE) | **8** datasets (7 VIGENTE) |
| ASFI: 3 datasets (2 VIGENTE) | **5** datasets (4 VIGENTE) |
| `balance_general`: «2 períodos atrasado» | **8** períodos (`tol=2`) |
| BCB: 5 datasets, todos VIGENTE | 5, correcto |
| Totales 21 / 16 / 2 / 3 / 0 | correcto |

Los totales cuadran por casualidad aritmética (10+3 = 8+5), que es justo por qué
esta clase de error pasa desapercibida. Cae en dos de las condiciones de fallo
del protocolo a la vez: evidencia parafraseada y un número que no se reproduce
con el comando del parte.

**Corrección pedida:** pegar la salida cruda del script (las 21 filas + la línea
de resumen), sin reformatear.

## R1 §3 · Observaciones

**O-1 · El parte describe un modelo que no es el que se commiteó.** §1 dice
«`DatasetLifecycleRecord`: Modelo **Pydantic inmutable**/serializable».
`lifecycle.py:47-60` es un `@dataclass` plano de la librería estándar, mutable
(sin `frozen=True`), y el módulo no importa Pydantic. La forma elegida está
bien; el problema es afirmar una propiedad —inmutabilidad— que el auditor no
puede dar por buena y que el código no tiene.

**O-2 · Cotejo de propuestas de herencia por subcadena.** `lifecycle.py:114-116`
acepta la correspondencia con `portal.lower() in orig_p` y
`dataset_id.lower() in orig_ds`. Una propuesta `ACEPTADA` para
`cuentas_nacionales_pib_trimestral` migraría también a `cuentas_nacionales_pib`.
D-18 descarta explícitamente la correlación léxica como criterio
(«'CADEX' y 'CADEXCO' son léxicamente casi idénticas y son entidades
distintas»); acá el riesgo es menor porque la propuesta ya pasó por aprobación
humana, pero el cotejo debería ser igualdad exacta de `(portal, dataset_id)`.
Hoy no tiene efecto medible: las 3 propuestas de B-55 están `RECHAZADA` y hay
0 `ACEPTADA`, así que la rama `MIG_D18_ACEPTADA` no se ejerce con datos reales
—solo en test—.

**O-3 · 7 de los 16 `VIGENTE` son datasets sin modelo de período.** Los que
salen con `Último Per. = -` (`memorias_institucionales`,
`gestion_administrativa_financiera`, `estadisticas_sociales_demograficas`,
`planes_estrategicos`, `documentos_institucionales_otros`,
`memoria_institucional`, `mercado_valores`,
`instituciones_financieras_desarrollo`) tienen `tolerance=0` y
`delay_periods=0`, y entran por `:136` (`0 <= 0`) con la justificación «Serie
vigente: retraso de 0 período(s) dentro de la tolerancia permitida (0)». Tienen
recursos reales (entre 8 y 386), no están vacíos, pero ahí `VIGENTE` significa
«no se evalúa vigencia», no «al día». Conviene un quinto valor o una marca
`sin_periodicidad`, para que el reporte no lea como que 16 series están al día
cuando 9 lo están y 7 no se miden. El parte tampoco hace esa distinción.

> **Nota de R2:** esta observación enumera ocho nombres y dice «7 de los 16».
> El reparto real es 8 y 8. Ver O-1 de R2.

**O-4 · Falta la verificación contra HEAD limpio.** Corresponde a todo bloque
que commitea Python. La corrí yo y salió en verde (§1); pero el parte debe
incluirla, no el acta.

**O-5 · Seguimiento sin actualizar.** `docs/plan_bloques_fase4.md:364` sigue
marcando B-56 como «Pendiente» y con la columna Real vacía. Lo actualizo al
cerrar la re-entrega, no antes.

**O-6 · Estado del árbol.** Mi re-corrida del script dejó
`docs/entregas/ciclo_vida_datasets.json` modificado (solo los 22 timestamps). No
lo restauré porque el comando de restauración requería aprobación; queda para
descartar con `git checkout HEAD -- docs/entregas/ciclo_vida_datasets.json` antes
de retomar el bloque.

## R1 §4 · Qué tiene que traer la re-entrega

1. **H-1** — `load_recovery_logs()` leyendo `recoveries` y derivando `success`
   del `status` real, + un test que use los JSON reales de B-54 y afirme
   `recovery_attempts_log` no vacío para `bcb/deuda_externa`. Si los fallos de
   la escalera no se persisten hoy en ningún artefacto, eso es el hallazgo real
   y va escrito en el parte como escalamiento, no sintetizado con `success`
   fijo.
2. **H-2** — `transition_date` estable entre corridas sin cambio de estado y
   `previous_state` poblado, con un test de dos corridas.
3. **H-3** — salida cruda del script pegada en el parte, y las cifras por portal
   corregidas (INE 8, ASFI 5, `balance_general` 8 períodos).
4. **O-1** — corregir la descripción del modelo en el parte.
5. **O-2** — cotejo por igualdad exacta de `(portal, dataset_id)`.

`O-3` queda a criterio: si se decide dejarlo como está, va anotado en
`AI_LOG.md` con el razonamiento, no en silencio.

No hay que rehacer el bloque. El núcleo —los cuatro estados, la invariante de
`HISTORICO`, la compuerta D-18, el control negativo— está bien y se queda.
