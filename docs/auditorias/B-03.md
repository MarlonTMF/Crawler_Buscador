# Auditoría B-03 (re-auditoría tras devolución)

- **Auditado por:** Claude
- **Fecha:** 2026-09-18
- **Parte revisado:** `docs/entregas/B-03.md` (versión corregida, commit `495c618`)
- **Veredicto:** **APROBADO**

## Contraste parte ↔ repositorio

```
$ git show --stat 495c618
 pyproject.toml                  | 5 +++++
 tests/test_validation_engine.py | 2 ++
 2 files changed, 7 insertions(+)
```

Exactamente lo pedido en la devolución: 7 inserciones, no 59. El diff
completo del archivo de tests son los dos decoradores, en los dos tests
correctos:

```
+@pytest.mark.live
 def test_http_fetcher_validate_url_access_caps_connection_error(monkeypatch):
...
+@pytest.mark.live
 def test_browser_fallback_distinguishes_document_from_landing_page(monkeypatch):
```

Y los dos tests de Gemini que habían viajado de polizón siguen en el árbol de
trabajo, no se perdieron:

```
$ git diff HEAD -- tests/test_validation_engine.py | grep "^+def test_"
+def test_http_fetcher_asks_gemini_for_alternative_urls(monkeypatch):
+def test_http_fetcher_asks_gemini_for_url_verdict(monkeypatch):
```

Quedan disponibles para B-07, junto al `fetcher.py` que los soporta.

## Sondeo

Lo único que importaba en esta re-auditoría era la parte que había fallado
antes: **¿HEAD está en verde contra sí mismo, no contra el árbol de
trabajo?** No tomé la palabra del parte — reproduje la verificación en un
worktree propio, independiente del que usó Antigravity:

```
$ git worktree add --detach <mi-worktree> HEAD
HEAD is now at 495c618 test: separa con el marcador live los 2 tests...

$ python -c "import crawler.core.fetcher as f; print(f.__file__)"
   ...\<mi-worktree>\src\crawler\core\fetcher.py      ← el del worktree, confirmado
   tiene _ask_gemini_for_alternatives? False           ← correcto: no está en HEAD

$ PYTHONPATH=<mi-worktree>/src python -m pytest tests/ -q -m "not live"
................................                                         [100%]
32 passed, 2 deselected in 10.51s

$ PYTHONPATH=<mi-worktree>/src python -m pytest tests/ -m "live" --collect-only -q
tests/test_validation_engine.py::test_http_fetcher_validate_url_access_caps_connection_error
tests/test_validation_engine.py::test_browser_fallback_distinguishes_document_from_landing_page
2/34 tests collected (32 deselected)
```

**Confirmado de forma independiente: HEAD está en verde, 0 fallos, y los dos
tests `live` son exactamente los correctos.** El número bajó de 52 a 32
porque HEAD todavía no tiene el resto de los cambios pendientes (discovery ya
está, pero `fetcher.py`, `validation_engine.py`, `headless_fetcher.py` y sus
tests asociados siguen sin commitear) — eso es exactamente lo esperado en
esta etapa del plan y no es un defecto de este bloque.

Segundo sondeo — que el marcador quedó bien registrado, no solo usado:

```
$ git show HEAD:pyproject.toml | grep -A3 pytest.ini_options
[tool.pytest.ini_options]
markers = [
    "live: toca red real o navegador real y puede tardar minutos...",
]
```

## Hallazgos

Ninguno.

## Qué se le devuelve a Antigravity

Nada. Bloque cerrado. Puede avanzar a B-04.

## Anotaciones para AI_LOG.md

Cierra el ciclo de E-05. La corrección fue exacta: separó con `git add -p`
sin que se perdiera nada, verificó contra HEAD limpio con `PYTHONPATH`
explícito —el paso exacto que a mí me había fallado en la primera pasada— y
lo dejó pegado en el parte de forma reproducible. Confirmar esto de forma
independiente (worktree propio, no reusar el suyo) costó tres comandos y dio
certeza real en vez de confianza prestada — es la misma razón por la que el
protocolo pide sondeo y no solo lectura del parte.
