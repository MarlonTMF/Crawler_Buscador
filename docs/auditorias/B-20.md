# Auditoría B-20 · Séptimo lote (IBCH) y cierre de onboarding de Etapa C

- **Auditado por:** Claude
- **Fecha:** 2026-09-18
- **Parte revisado:** `docs/entregas/B-20.md` (commit `f4bcb0e`)
- **Commit auditado:** `f663745` — `feat(B-20): onboarding 25 portal IBCH y consolidacion de catalogo`
- **Pasada:** primera
- **Veredicto:** **DEVUELTO** *(subsanado — ver §8, segunda pasada: **APROBADO CON OBSERVACIONES**)*

**La mitad de onboarding del bloque está impecable y no se rehace.** IBCH reproduce
exactamente —26 filas, 11/15 por dataset, 0 errores— y el pedido §5 de la auditoría de
B-19 se cumplió a la letra: descargué los bytes de las **26** URLs y obtuve los mismos
dos números que el parte, `11 filas / 10 documentos` y `15 filas / 14 documentos`, con las
dos páginas HTML identificadas correctamente. Es el primer parte del proyecto en que el
chequeo por bytes viene medido y no ejemplificado.

Lo que devuelve el bloque es la otra mitad, la que le da el título: **el "Balance
Consolidado de la Etapa C" (§4) no reproduce en 13 de sus 26 filas, su total está 158
recursos por debajo de lo que hay en las bases, y la tabla ni siquiera suma su propio
total.** Esa tabla es el artefacto que el bloque entrega como cierre de etapa y es el que
va a alimentar el informe de B-26. Se suman dos declaraciones de §2 que el repositorio no
respalda (O-22 y CEPAL).

---

## 1. Lo que se sostiene

### IBCH — totales, desglose y estado (contados sobre `inventory.db`)

```
$ python - (sqlite3 output/ibch/inventory.db)
n=26  uniq_download_url=26
Counter({'manuales_normas_construccion': 15, 'boletines_tecnicos_cemento': 11})
status  Counter({'PROCESADO_EXITOSAMENTE': 26})
error   Counter({None: 26})
corrida 2026-09-18T23:42:15Z → 23:43:01Z   source_inventory.updated_at 23:41:02Z
```

Coincide fila por fila con §3 del parte: 26 · 26 · `PROCESADO_EXITOSAMENTE: 26` · 0
errores, 11 y 15 por dataset. Sin duplicados. La corrida es posterior a las de B-19
(23:27) y no se solapa con ninguna.

### El chequeo por bytes — reproduce, y con las 26, no con una muestra

```
$ python - (urllib, Range: bytes=0-2047, path normalizado)
=== boletines_tecnicos_cemento  11
   10× 206 application/pdf  %PDF-1.7 ×5, %PDF-1.6 ×5   (a13n13 … a23n23)
    1× 200 text/html <!DOCTYP   https://www.ibch.com/boletin/            (semilla)
=== manuales_normas_construccion 15
   14× 206 application/pdf  %PDF-1.7
    1× 200 text/html <!DOCTYP   https://www.ibch.com/download/estatutos-ibch/
```

**24 documentos reales de 26 filas, ni un 404, ni un timeout.** Los dos números por
dataset del parte son exactos y las dos filas que el parte declara como HTML son
exactamente las dos que yo encontré. Las cuatro muestras citadas en §3 (`a23n23.pdf`,
`a22n22.pdf`, `FT_Arena_IBCH-Tipo-Ottawa.pdf`, `Certificado_IBNORCA_2026…pdf`) dan
`206 / %PDF-1.7` tal como el parte dice.

El contenido es lo que los nombres dicen: boletines técnicos del IBCH a13–a23, manuales de
pavimento rígido / suelo-cemento / reciclado, fichas técnicas y certificados IBNORCA de
arena patrón. Institución correcta y única (D-01): `ibch.com` es el Instituto Boliviano
del Cemento y el Hormigón, sin homónimo en el catálogo.

### Calibración, catálogo e higiene

```
$ grep max_depth/max_pages config/source_ibch.yaml   → max_depth: 1 · max_pages: 35
$ git show --stat f663745
 config/source_ibch.yaml           |  90 +++
 output/excel_urls_diagnostic.json |  20 +-
 2 files changed, 101 insertions(+), 9 deletions(-)
$ git show --stat f4bcb0e
 docs/entregas/B-20.md | 134 +++
```

Archivos por nombre, sin `git add -A`, parte en su propio commit `docs:`. El diff del
catálogo son seis entradas y son **exactamente** las seis que §2 declara: IBCH (+source
+dataset), ICCO→null, MDRyT→null, MDRyT/OAP→null, MMYM→`gestion_institucional_minera`,
SNIS→`publicaciones_salud`. Ni una más.

Las dos correcciones de dataset son las correctas y siguen el segundo pedido §5 de B-19
(el representante debe ser el dataset con más documentos verificados): SNIS pasa de
`boletines_epidemiologicos` (1 documento real de 5) a `publicaciones_salud` (52), MMYM a
`gestion_institucional_minera` (84). **O-19 y O-8/O-18 quedan cerrados.**

### Tests

`git diff --stat f663745 HEAD` → solo `docs/entregas/B-20.md`. El árbol de trabajo es
idéntico en código a `f663745`, así que el paquete editable importa el commit auditado:

```
$ python -m pytest tests/ -q -m "not live"
......................................................................   [100%]
70 passed in 7.72s
```

El `70 passed` del worktree aislado del parte se sostiene (E-05 cubierto).
*(No pude levantar mi propio worktree — el comando quedó sin aprobación — pero la
equivalencia árbol≡commit hace la verificación válida igual.)*

---

## 2. Sondeo — el balance consolidado de §4, contado sobre las 26 bases

El sondeo de esta acta no es IBCH (eso lo verifiqué entero arriba): es **la tabla de §4**,
porque es lo que el bloque entrega como cierre de etapa. Conté `resource_audit_log` y
`count(distinct dataset_id)` en las 26 `inventory.db`.

```
$ python - (sqlite3 sobre output/<src>/inventory.db, las 26)
finrural n= 257 ds=1   bcp n=  47 ds=2   ine n= 16 ds=2   asfi n= 73 ds=5
bcb      n=  16 ds=3   aps n=  13 ds=2   ada n=  5 ds=2   dgac n= 15 ds=1
seprec   n=  14 ds=2   ibce_cao n=30 ds=1  cndc n= 94 ds=1  asofin n=25 ds=1
att      n=  67 ds=2   ae  n= 116 ds=2   anapo n=100 ds=1  atc n= 42 ds=2
mmym     n= 108 ds=3   an  n= 204 ds=3   senamhi n=801 ds=4  cadexco n=10 ds=2
fam      n= 111 ds=3   in  n= 248 ds=3   snis n= 63 ds=3   cadeco n= 12 ds=2
min_educacion n=15 ds=2  ibch n= 26 ds=2
TOTAL 2528   datasets 57
```

| # | Portal | §4 dice (ds · rec) | Contado (ds · rec) | |
|---|---|---|---|---|
| 1 | FINRURAL | 3 · 129 | 1 · **257** | ✗ |
| 2 | BCP | 2 · 25 | 2 · **47** | ✗ |
| 3 | INE | 1 · 9 | **2** · **16** | ✗ |
| 4 | ASFI | 4 · 42 | **5** · **73** | ✗ |
| 5 | BCB | 1 · 13 | **3** · **16** | ✗ |
| 6 | APS | 2 · 70 | 2 · **13** | ✗ |
| 7 | ADA | 1 · 2 | **2** · **5** | ✗ |
| 8 | DGAC | 3 · 68 | **1** · **15** | ✗ |
| 9 | SEPREC | 1 · 20 | **2** · **14** | ✗ |
| 10 | IBCE-CAO | 2 · 16 | **1** · **30** | ✗ |
| 11 | CNDC | 3 · 42 | **1** · **94** | ✗ |
| 12 | ASOFIN | 1 · 18 | 1 · **25** | ✗ |
| 13 | ATT | 2 · 58 | 2 · **67** | ✗ |
| 14–26 | AE · ANAPO · ATC · MMYM · AN · SENAMHI · CADEXCO · FAM · IN · SNIS · CADECO · MIN_EDUCACION · IBCH | — | idénticos | ✅ |
| | **TOTAL** | 58 · **2 370** | **57** · **2 528** | ✗ |

**El corte es limpio y dice de dónde vienen los números.** Las 13 filas que reproducen son
las de B-17 a B-20 —los cuatro lotes que audité contando sobre la base—. Las 13 que no
reproducen son todas de B-14, B-15 y B-16. No es deriva de corridas nuevas: la auditoría
de B-15 (`docs/auditorias/B-15.md:17-21`) ya había contado `asfi 73 · aps 13 · ada 5 ·
dgac 15 · seprec 14` **el mismo día**, y es exactamente lo que hay ahora. Los `70` de APS
y los `68` de DGAC de §4 no salieron nunca de ninguna base.

Y hay un error aritmético anterior a todo eso: **las 26 filas de §4 suman 2 368, no los
2 370 que la propia tabla declara como total.** Una tabla que no suma su propio total no
se contó; se transcribió.

### Corolario: el `resource_audit_log` sí acumula corridas

```
$ python - (group by download_url having count(*)>1)
finrural  2 filas duplicadas   ventana 15:06 → 15:11
bcp      11 filas duplicadas   ventana 15:12 → 15:57   (dos corridas en la misma db)
asfi      5 filas duplicadas   ventana 16:00 → 19:45   (la segunda, durante B-20)
```

Esto corrige lo que di por bueno en la auditoría de B-19 ("el `resource_audit_log` no
arrastra filas de corridas viejas"): no las arrastra **si la fuente se corre una sola
vez**. Al re-correr, las filas se suman. De los 2 528 contados, 18 son filas repetidas:
**2 510 recursos únicos**. Es la cifra que debería ir al informe de B-26, y ninguna de las
tres —2 370, 2 528, 2 510— está hoy escrita en ningún lado con el comando que la produce.

---

## 3. Observaciones

**O-25 · §4 publica 13 cifras de 26 que no reproducen, y una tabla que no suma su propio
total.** Detalle completo arriba. Es la observación que devuelve el bloque: el protocolo
lista *"un número que no se puede reproducir corriendo el comando del parte"* entre las
seis cosas que hacen fallar una auditoría, y acá no es un número, son trece más el total.
Agrava que la dirección del error no es uniforme —APS y DGAC están inflados ×5 y ×4,
CNDC e IBCE-CAO están subcontados a la mitad—, así que no hay una corrección global: hay
que **contar las 26**.

**O-26 · O-22 se declara subsanado y quedó una fila de las cuatro.** §2 dice *"Se
revertieron a `null` las asignaciones prematuras de `icco`, `mdryt` y `mdryt/oap`"*. El
diff revierte tres filas; la auditoría de B-19 listaba cuatro:

```
$ python - (filas con crawler_source vs. existencia de output/<src>/inventory.db)
34 filas con crawler_source sobre 67
  huérfanas: [('FDTA-Valles', 'icco', 'programas_fdta_valles')]
$ ls config/ | grep -i "icco"   → (vacío)
```

`FDTA-Valles` sigue marcada como onboardeada con `crawler_source: "icco"` sin YAML y sin
base. La cobertura efectiva de Track B es **33 filas con respaldo sobre 34 marcadas**, no
34/34 como sostiene el "100% alineado con las corridas reales" de §7. Es una línea de
corrección, pero es exactamente la clase de residuo que O-17 → O-22 viene arrastrando
desde B-15: se subsana el caso citado, no la clase.

**O-27 · La decisión sobre CEPAL se declara registrada en el catálogo y no está en ningún
lado.** §2 dice *"Queda registrada en el catálogo con nota de arquitectura específica para
futura atención vía conector DSpace API especializado (Fase 4)"*.

```
$ git show f663745 -- output/excel_urls_diagnostic.json   → CEPAL no aparece en el diff
$ python - (fila CEPAL del catálogo)                      → sin nota, sin campo nuevo
$ grep -rn "CEPAL\|DSpace" docs/decisiones.md             → (vacío)
```

La fila de CEPAL está igual que antes del bloque. Dos problemas, uno de evidencia y uno de
proceso:

1. **De evidencia:** una declaración de §2 sin nada detrás. Es la misma forma que el
   proyecto tiene registrada cuatro veces — verificar el artefacto declarado, no el efecto.
2. **De proceso:** el protocolo (§"Condiciones de parada", primera fila) dice que escribir
   contenido de `docs/decisiones.md` es condición de parada y que la propuesta va en
   *"Dudas / escalamientos"*, no al commit, **"aunque el texto parezca de bajo riesgo"**.
   Una "exclusión técnica justificada" declarada formalmente es precisamente eso. §7 dice
   "Ninguna".

La decisión en sí la confirmo —ya la tomé yo en `docs/auditorias/B-17.md:392-410` y no
cambia—, pero **tiene que quedar escrita donde vive, no afirmada donde no está.**

**O-28 · `resource_audit_log` acumula filas al re-correr una fuente** (18 filas repetidas
hoy). No es de este bloque y no afecta al criterio, pero toca cualquier cifra agregada:
el número de cobertura hay que tomarlo con `count(distinct download_url)`. Va a B-26 junto
con O-14.

**O-29 · Sigue abierto lo de siempre.** Los YAML de B-14/B-15 conservan `max_depth: 0` /
`max_pages: 5` (hallazgo 1 de la auditoría de B-15, sin cerrar); `filename_pattern` sigue
inerte en `source_ibch.yaml` (O-15/O-23); `content_sha256` sigue NULL en las 26 filas de
IBCH, que es por qué este chequeo por bytes lo tengo que hacer desde afuera del pipeline
en cada auditoría (O-16/O-24). Nada de esto es de B-20; queda donde ya estaba, en B-24 y
en el bloque propio de verificación de bytes.

*Menor, sin número:* §3 rotula el total como `24 documentos reales por bytes (%PDF-1.7)` y
cinco de los boletines son `%PDF-1.6`. No cambia ninguna cifra; el rótulo generaliza la
muestra.

---

## 4. Sobre el estimado y el parte

Estimado 40 min (`plan_bloques.md:115`), real 35, desvío −12,5 %, informado en porcentaje.
El encabezado trae ejecutor, fecha, commit y criterio copiado; §2 declara desviaciones;
§5 pega la corrida de tests en worktree aislado con `PYTHONPATH` (E-05). La parte de
onboarding —§3— es la mejor escrita de los siete lotes: dos números por dataset, muestras
con status y bytes mágicos, y las filas HTML identificadas en vez de omitidas.

El problema es de forma inversa al habitual: **el parte no adornó lo que midió, adornó lo
que no midió.** Todo lo que corresponde a la corrida de IBCH lo verifiqué y es exacto;
todo lo que corresponde a consolidar el trabajo de otros bloques —§4, la mitad de §2— está
transcrito o afirmado. Y §7 cierra con *"el catálogo queda limpio, 100 % alineado con las
corridas reales"*, que es la única frase del parte que contradice directamente lo que
devuelve el `inventory.db`.

---

## 5. Qué hay que corregir para que B-20 cierre

No se rehace el bloque. **El onboarding de IBCH está aprobado y no se toca.** Tres
correcciones, todas sobre documento y catálogo:

1. **Recontar §4 con un comando y pegar su salida.** Una corrida de
   `select count(*), count(distinct download_url), count(distinct dataset_id) from
   resource_audit_log` sobre las 26 bases, con la salida pegada en el parte y la tabla
   generada de ahí. El total va con recursos **únicos**. Si la tabla no suma su propio
   total, no se contó.
2. **Revertir `FDTA-Valles` a `crawler_source: null` / `crawler_dataset: null`**, con lo
   que O-22 sí queda cerrado como clase, y corregir la cifra de §7 (33/34, o 34/34 después
   de la reversión — pero medida, no afirmada).
3. **Escribir la decisión de CEPAL en `docs/decisiones.md`** con el formato de siempre
   (Contexto → Alternativas descartadas → Decisión → Razón → Consecuencia → Umbral que la
   reabriría → Verificado el), citando `docs/auditorias/B-17.md:392-410` como origen; **o**
   agregar efectivamente la nota al catálogo si eso es lo que se quiso decir. Lo que no
   puede quedar es la declaración sin el artefacto. Va en su propio commit `docs:`.

El commit de corrección es `fix(B-20): …` sobre el catálogo y `docs:` sobre el parte; no
hay que re-correr ninguna fuente.

## 6. Qué queda anotado

Para **B-24**: O-29 (`max_depth: 0` de B-14/B-15, `filename_pattern` inerte), O-20
(semillas como recursos — IBCH aporta 2 más), O-12, O-1/O-7.
Antes de **B-26**: O-25 (el balance recontado), O-26 (`FDTA-Valles`), O-28 (contar por
`distinct download_url`), O-14, O-6, O-21.
Como **bloque propio**: E-12 + E-13 + O-16 + O-24 — verificación de bytes dentro del
pipeline. IBCH es el séptimo lote seguido en que la auditoría la hace a mano desde afuera.

`docs/plan_bloques.md:115` → B-20: queda **⬜** hasta la corrección.

---

## 7. Anotaciones para `AI_LOG.md`

**E-15 · Consolidar no es copiar: una tabla que no suma su propio total no se contó.** El
"Balance Consolidado de la Etapa C" de B-20 tiene 13 de 26 filas que no reproducen contra
`inventory.db`, y el corte es exacto: las 13 correctas son las de los cuatro lotes cuyas
cifras yo había contado en una auditoría; las 13 incorrectas son las de los tres lotes
anteriores. Los `70` de APS y los `68` de DGAC no salieron de ninguna base — la auditoría
de B-15 ya había contado 13 y 15 el mismo día. El error se delata solo: las filas suman
2 368 y la tabla declara 2 370. **Una cifra verificada en su bloque no queda verificada
para siempre; al re-publicarla en un agregado hay que volver a correr el comando**, porque
el agregado es un artefacto nuevo y hereda el estado de verificación de cómo se armó, no
el de sus partes. El corolario operativo es barato: si la tabla no suma su propio total,
se transcribió.

**E-16 · Declarar un registro no es registrarlo.** §2 de B-20 dice que la exclusión de
CEPAL "queda registrada en el catálogo con nota de arquitectura específica". El diff no
toca CEPAL, la fila no tiene nota y `docs/decisiones.md` no la menciona. Es la quinta
aparición de la misma forma —verificar el artefacto declarado y no el efecto— pero con una
vuelta nueva: acá el artefacto no es una URL ni un log, **es una frase del propio parte**,
y la única manera de detectarlo es buscar en el repositorio lo que el parte dice haber
escrito. Se suma que el protocolo ya prohíbe decidir eso sin escalar: la decisión era
correcta y aun así el camino estaba mal, que es exactamente el caso que la condición de
parada anticipa cuando dice "aunque el texto parezca de bajo riesgo".

**E-17 · `resource_audit_log` acumula al re-correr.** En la auditoría de B-19 escribí que
no arrastra filas de corridas viejas; era cierto para fuentes corridas una sola vez. BCP
tiene 11 filas repetidas de dos corridas (15:12 y 15:57) y ASFI 5 (16:00 y 19:45). Toda
cifra agregada de cobertura va con `count(distinct download_url)`, no con `count(*)`.

---
---

# Auditoría B-20 · Segunda pasada (subsanación)

- **Auditado por:** Claude
- **Fecha:** 2026-09-18
- **Parte revisado:** `docs/entregas/B-20.md` (reescrito en `59fbec1`)
- **Commits auditados:** `f663745` (onboarding IBCH + catálogo) · `7012b9c` (subsanación de catálogo) · `59fbec1` (D-10 + parte recontado)
- **Pasada:** segunda
- **Veredicto:** **APROBADO CON OBSERVACIONES**

Las tres correcciones que pedía §5 de la primera pasada están hechas y **las tres reproducen
contra el repositorio**, no contra el parte. El balance de §4 ya no es una transcripción:
conté las bases por mi cuenta y sale la misma cifra hasta la última fila. Lo que queda
anotado no devuelve el bloque —una explicación causal equivocada en una nota al pie, un
directorio huérfano, un commit que falta en la lista— pero uno de los tres destapa un hueco
de la tubería que hoy no está en ningún bloque.

---

## 8. Verificación de las tres correcciones pedidas

### 8.1 · O-25 — el balance recontado (CERRADO)

Conté las bases yo, sin partir del parte, con la consulta que el parte declara:

```
$ python - (sqlite3 sobre output/*/inventory.db: count(*) / count(distinct download_url) / count(distinct dataset_id))
ada             rows=   5 uniq=   5 ds=2
ae              rows= 116 uniq= 116 ds=2
an              rows= 204 uniq= 204 ds=3
anapo           rows= 100 uniq= 100 ds=1
aps             rows=  13 uniq=  13 ds=2
asfi            rows=  73 uniq=  68 ds=5   DUP
asofin          rows=  25 uniq=  25 ds=1
atc             rows=  42 uniq=  41 ds=2   DUP
att             rows=  67 uniq=  67 ds=2
bcb             rows=  16 uniq=  16 ds=3
bcp             rows=  47 uniq=  36 ds=2   DUP
cadeco          rows=  12 uniq=  12 ds=2
cadexco         rows=  10 uniq=  10 ds=2
cndc            rows=  94 uniq=  94 ds=1
dgac            rows=  15 uniq=  15 ds=1
fam             rows= 111 uniq= 111 ds=3
finrural        rows= 257 uniq= 255 ds=1   DUP
ibce_cao        rows=  30 uniq=  30 ds=1
ibch            rows=  26 uniq=  26 ds=2
in              rows= 248 uniq= 248 ds=3
ine             rows=  16 uniq=  16 ds=2
mefp            rows=   0 uniq=   0 ds=0
min_educacion   rows=  15 uniq=  15 ds=2
mmym            rows= 108 uniq= 105 ds=3   DUP
senamhi         rows= 801 uniq= 801 ds=4
seprec          rows=  14 uniq=  14 ds=2
snis            rows=  63 uniq=  63 ds=3
N bases=27 TOTAL rows=2528 uniq=2506 datasets=57
```

**Las 26 filas de §4 reproducen, una por una.** Las 13 que fallaban en la primera pasada
—FINRURAL 257, BCP 47, INE 16/2ds, ASFI 73/5ds, BCB 16/3ds, APS 13, ADA 5/2ds, DGAC 15/1ds,
SEPREC 14/2ds, IBCE-CAO 30/1ds, CNDC 94/1ds, ASOFIN 25, ATT 67— ahora coinciden con la base.
Los `70` de APS y los `68` de DGAC desaparecieron.

Y la tabla **suma su propio total**, que era el corolario operativo de E-15. Lo verifiqué en
las tres columnas:

| Columna | Suma de las 26 filas | Total declarado en §4 | |
|---|---|---|---|
| Filas en DB | 2 528 | 2 528 | ✅ |
| Recursos únicos | 2 506 | 2 506 | ✅ |
| Datasets | 57 | 57 | ✅ |

El total se publica además en **recursos únicos** (2 506), y la nota al pie lo señala como
"la cifra neta y real de cobertura para B-26". Era el pedido exacto.

**Corrección a mi propia acta anterior:** escribí "18 filas repetidas → 2 510 recursos
únicos". Son 22 → **2 506**. Conté los duplicados de FINRURAL (2), BCP (11) y ASFI (5) y me
faltaron MMYM (3) y ATC (1), porque en la primera pasada corrí `count(*)` sobre las 26 bases
pero `distinct` solo sobre las que ya sabía re-corridas. El parte tiene la cifra correcta y
yo no. Es la misma lección que le observé a §4, aplicada a mí: conté una parte y extrapolé
el resto.

### 8.2 · O-26 — `FDTA-Valles` (CERRADO)

```
$ git show 7012b9c -- output/excel_urls_diagnostic.json
-    "crawler_source": "icco",
-    "crawler_dataset": "programas_fdta_valles"
+    "crawler_source": null,
+    "crawler_dataset": null
```

```
$ python - (todas las filas con crawler_source vs. existencia de config/source_<src>.yaml y output/<src>/inventory.db)
total entradas: 67
con crawler_source: 33
huerfanas: []
fuentes distintas: 26
```

**33 entradas marcadas, 33 con YAML y base, 0 huérfanas, 26 fuentes distintas.** Es la cifra
que §2 y §6 del parte declaran, y sale de correr la comprobación sobre las 67 filas, no
sobre las citadas: O-22 queda cerrado **como clase**, no como caso.

### 8.3 · O-27 — CEPAL (CERRADO, y por los dos caminos)

La primera pasada pedía D-10 escrita **o** la nota en el catálogo. Están las dos:

```
$ grep -n "D-10" docs/decisiones.md
380:## D-10 · CEPAL: exclusión técnica justificada de Track A por arquitectura DSpace
```

El formato es el del proyecto y está completo: Origen → Contexto → Alternativas consideradas
(dos, con el porqué del descarte) → Decisión → Razón → Consecuencia → **Umbral que reabriría
esto** → Verificado el (2026-09-18). Cita `docs/auditorias/B-17.md:392-410` como origen, que
es de donde salió, y el umbral es comprobable ("si CEPAL expone enlaces directos descargables
en HTML estático, o cuando se abra la Fase 4").

```
$ python - (fila CEPAL del catálogo)
CEPAL | src= None | ds= None | nota= True
   technical_exclusion_note: "Exclusion tecnica justificada de Track A (B-17 / B-20):
   repositorio DSpace (repositoriodigital.cepal.org) requiere conector API REST OAI-PMH;
   no integrable via prospeccion HTML BFS estandar. Programado para Fase 4."
```

El catálogo sigue siendo JSON válido —`json.load` lo abre— así que el campo nuevo
`technical_exclusion_note` no repitió el incidente de la coma colgante de `moved_urls.json`.

El camino de proceso también se corrigió: la decisión no entró escondida en el commit de
ejecución, va en **`59fbec1`, un commit `docs:` propio**, y el parte la declara en §6 "Dudas
y escalamientos" citando la condición de parada. Era lo que señalaba E-16.

### 8.4 · Higiene de commits y tests

```
$ git show --stat f663745   → config/source_ibch.yaml (+90) · output/excel_urls_diagnostic.json (20 +/-)
$ git show --stat 7012b9c   → output/excel_urls_diagnostic.json (+6 -3)
$ git show --stat 59fbec1   → docs/auditorias/B-20.md (+317) · docs/decisiones.md (+15) · docs/entregas/B-20.md (+158 -58)
$ git status --short        → (vacío)
$ git diff --stat HEAD      → (vacío)
```

Archivos por nombre, sin `git add -A`, tres commits que separan ejecución / corrección de
datos / decisión-documentación. El árbol es idéntico a HEAD, así que el paquete editable
importa el commit auditado (E-05):

```
$ python -m pytest tests/ -q -m "not live"
......................................................................   [100%]
70 passed in 7.51s
```

Coincide con el `70 passed in 8.47s` del worktree aislado de §5 del parte.

### 8.5 · IBCH — sin cambios, sigue aprobado

`config/source_ibch.yaml` conserva `max_depth: 1` / `max_pages: 35`, y la base sigue en 26
filas / 26 únicas / 2 datasets / 0 errores. El onboarding quedó aprobado en la primera pasada
y no se tocó. §3 además corrigió el rótulo que observé al pasar: ahora dice "19 archivos
`%PDF-1.7` y 5 `%PDF-1.6`", que es exactamente lo que medí (10 PDFs en
`boletines_tecnicos_cemento`, cinco de cada versión; 14 en `manuales_normas_construccion`,
todos 1.7).

---

## 9. Observaciones nuevas

**O-30 · La nota al pie de §4 explica mal 6 de los 22 duplicados, y una fecha va al revés.**
La cifra 2 506 es correcta; la frase que la acompaña no. §4 dice que las 22 filas de
discrepancia *"provienen de re-ejecuciones que acumularon corridas en la misma base **antes
de B-17**"*. Miré el `execution_timestamp` de cada URL repetida:

```
$ python - (group by download_url having count(*)>1, con min/max execution_timestamp)
asfi     n=2  16:01:01 .. 19:45:15   /ef/educacion-financiera          (dos corridas)
bcp      n=2  15:12:11 .. 15:56:59   /web/institucional/boletines...   (dos corridas)
finrural n=2  15:06:25 .. 15:08:44   /archivos/gri/reporte-sosten...   (MISMA corrida, 2m19s)
mmym     n=2  21:25:20 .. 21:25:45   /uploads/2026/04/3.-PPT_RPCI      (MISMA corrida, 25s)
atc      n=2  21:12:14 .. 21:12:58   /sobre-red-enlace/documentos...   (MISMA corrida, 44s)
```

Dos cosas: la segunda corrida de ASFI es de las **19:45**, o sea *durante* B-20 (los commits
del bloque son 19:44–19:49), no antes de B-17; y los 6 duplicados de FINRURAL (2), MMYM (3) y
ATC (1) **no son re-ejecuciones en absoluto** — las dos filas caen dentro de la ventana de
una sola corrida, separadas por 25 s a 2 min.

**O-31 · Hay un hueco de deduplicación dentro de una misma corrida.** Es el corolario de O-30
y es lo que importa: el motor registra dos veces la misma `download_url` en una sola pasada,
alcanzada por dos caminos distintos del BFS. No es artefacto de re-correr, es la tubería.
Hoy afecta a 3 de las 26 fuentes y a 6 filas, pero es la clase de error que crece con la
profundidad — y **B-24 es justamente el bloque que va a subirla, así que O-31 entra ahí como
precondición, no como nota.** Mientras no se cierre, toda cifra agregada se toma con
`count(distinct download_url)` (O-28).

**O-32 · `output/mefp/` existe con una base vacía, sin YAML y sin entrada marcada.** El
recuento encuentra **27** directorios con `inventory.db`, no 26: `mefp` tiene `inventory.db`
con 0 filas más `mapa_mefp.json`, `mapa_mefp_compact.json`, `mapa_mefp_tree.json` y
`alerts/`. No existe `config/source_mefp.yaml` y la fila MEFP del catálogo tiene
`crawler_source: null`. No afecta ninguna cifra —suma cero— y `output/` no está versionado
(solo los tres JSON del catálogo lo están), así que no ensucia el repositorio. Es residuo de
una corrida abandonada. Queda anotado para que el runner de B-26 no lo cuente como fuente
vacía y para que nadie lo borre sin mirar: **si fue un intento de onboarding de MEFP que
falló, eso es información y su lugar es el parte del bloque que lo intentó, no el disco.**

**O-33 · §7 del parte lista 2 commits y el bloque tiene 3.** Falta `59fbec1`, que es
precisamente el que contiene D-10, el artefacto que §2 y §6 declaran haber escrito. Es
recursivo (el parte vive en ese commit) y se resuelve con una línea en el próximo parte:
listar el hash del commit `docs:` aunque sea el que lo contiene. Sin eso, quien lea solo §7
no encuentra la decisión.

**O-34 · Verificado al pasar, sin hallazgo.** El `download_url` de las 47 filas de `bcp` es
`www.bcp.gov.py` — Banco Central del **Paraguay**. Levanté la bandera D-01 y no corresponde:
la entrada del catálogo dice `"Institucion": "Banco Central del Paraguay"` y
`"Url_Original": "https://www.bcp.gov.py/"`, y `config/source_bcp.yaml` declara
`name: "Banco Central del Paraguay"`. YAML, catálogo y base apuntan a la misma institución;
no es el `bcp.org` del incidente de D-01. Lo dejo escrito para que la próxima auditoría no
gaste el sondeo dos veces en lo mismo.

**O-29 sigue abierto tal cual** (`max_depth: 0` en los YAML de B-14/B-15, `filename_pattern`
inerte —en `source_ibch.yaml` es el TODO literal del generador—, `content_sha256` NULL).
Nada de esto es de B-20.

---

## 10. Sobre el estimado

Estimado 40 min (`plan_bloques.md:115`), real 45, desvío +12,5 %, informado en porcentaje. La
primera pasada declaraba 35 min / −12,5 %; los 10 minutos de diferencia son el trabajo de
subsanación, y está bien que el parte los absorba en vez de abrir un bloque nuevo: la
corrección fue de documento y catálogo, sin re-correr ninguna fuente, tal como indicaba §5.

---

## 11. Qué queda anotado

Para **B-24**: **O-31 (dedup intra-corrida), como precondición de subir la profundidad**,
O-29 (`max_depth: 0` de B-14/B-15, `filename_pattern` inerte), O-20 (semillas contadas como
recursos), O-12, O-1/O-7.
Antes de **B-26**: O-28 (contar por `distinct download_url` — la cobertura es **2 506**, no
2 528), O-32 (`output/mefp/` vacío), O-14, O-6, O-21.
Como **bloque propio**: E-12 + E-13 + O-16 + O-24 — verificación de bytes dentro del
pipeline.
De forma, para el próximo parte: O-33 (listar todos los commits del bloque) y O-30 (una
explicación causal también se verifica, no solo la cifra que acompaña).

**Cifras validadas, aptas para viajar a B-26:** 26 fuentes con base · **2 506 recursos
únicos** (2 528 filas) · 57 datasets · 33/33 entradas del catálogo respaldadas · IBCH 26
filas / 24 documentos reales por bytes.

`docs/plan_bloques.md:115` → B-20 pasa a **✅**, real 45 min, commit `f663745`.
**La Etapa C queda cerrada.**

---

## 12. Anotaciones para `AI_LOG.md`

Se escriben **E-15**, **E-16** y **E-17** (propuestas en §7 de la primera pasada). E-17 va
corregida: son 22 filas repetidas y no 18, y el mecanismo no es solo la re-ejecución.
