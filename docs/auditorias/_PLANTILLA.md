# Auditoría B-NN

- **Auditado por:** Claude
- **Fecha:** YYYY-MM-DD
- **Parte revisado:** `docs/entregas/B-NN.md`
- **Veredicto:** APROBADO | APROBADO CON OBSERVACIONES | DEVUELTO

## Contraste parte ↔ repositorio

```
$ git show --stat <hash>
```

¿Los archivos tocados coinciden con lo que el parte declara? Si hay archivos
de más o de menos, decirlo acá.

## Sondeo

Qué detalle concreto se verificó de forma independiente (distinto en cada
bloque — no repetir siempre el mismo), y qué mostró.

```
$ comando del sondeo
salida
```

## Hallazgos

Numerados, del más importante al menos. Si no hay ninguno, decir "ninguno" —
no rellenar para dar volumen.

## Qué se le devuelve a Antigravity

Solo si el veredicto es DEVUELTO: la lista concreta de lo que hay que
corregir. No se rehace el bloque entero, se corrige lo señalado.

## Anotaciones para AI_LOG.md

Si el bloque produjo un aprendizaje que vale para los siguientes (una
estimación que se desvió mucho y por qué, un supuesto que no se sostuvo),
indicarlo acá para que entre a la bitácora.
