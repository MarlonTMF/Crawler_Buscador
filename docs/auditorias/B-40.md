# Acta de Auditoría: B-40 · Corrida completa y cierre de Fase 2

**Fecha:** 2026-09-21  
**Auditor:** Claude (asistido técnicamente por Antigravity para la persistencia)  
**Estado:** **DEVUELTO** en Ronda 1 y Ronda 2 (en proceso de subsanación técnica)  

---

## Ronda 1 (Commit `0c34c93`) — Veredicto: DEVUELTO

> **Limitación declarada:** En esta sesión no pude ejecutar Python. Me apoyé en el parte, git, lectura estática del comparador y tiempos de archivo.

### El sondeo que decide
El bloque pide "correr todos los portales de punta a punta" y el parte afirma haberlo hecho. La base `inventory.db` más reciente de todo `output/` es `sicoes` a las **10:31 del 21-sep**; las bases de los 22 portales del benchmark son del 18 y 19 de septiembre. La corrida completa no ocurrió y el parte no lo declara (la sección "Desviaciones del plan" falta).

### Segundo motivo
El criterio exige los cuatro objetivos medidos y pegados literales. Solo el objetivo 3 trae comando y salida. Las cifras 10.985, 46 fuentes y 100% SHA-256 se afirman sin comando. La de integridad además no puede venir del comparador: su consulta filtra extensión D-14 y estado, no mira `file_size_bytes` ni hash.

### Se devuelven tres cosas
1. Decidir y declarar qué pasa con la corrida: correrla de verdad o decir explícitamente que se mide sobre el estado acumulado al 21-sep 10:31 y por qué alcanza.
2. Pegar comandos y salidas de los objetivos 1, 2 y 4: con `file_size_bytes` y hash en el 4.
3. Nombrar las 7 fuentes del bucket "sin pipeline binario directo" fijando un solo denominador.

---

## Ronda 2 (Commit `2e68543`) — Veredicto: DEVUELTO

> **Limitación declarada:** Igual que en la Ronda 1, no pude correr Python por permisos de ejecución en esta sesión. Lectura estática de scripts, `git show --stat`, listado de `output/` y mapas JSON.

### Lo que sí quedó subsanado
- **Punto 1 (la corrida):** declarada como desviación deliberada con sus tres razones. Es la salida correcta: el acumulado al 21-sep 10:31 es reproducible, una re-corrida en vivo no lo es. Aceptado.
- **Punto 2:** existe `scripts/medir_objetivos_fase2.py` commiteado, con salida pegada.
- **Menores:** defaults `103`/`18` eliminados; `test_comparador_benchmark.py:104` pasó a `== 5411`; `plan_bloques_fase2.md:296` volvió a Claude; el +3.384 se atribuye al conjunto B-28/B-30/B-31/B-32.
- **Sondeo:** revisados los directorios sospechosos de `output/` (`an`, `in`, `cadeco`, `otros_historicos`, `min_educacion`). No son basura: `an` es Aduana Nacional e `in` es Impuestos Nacionales, ambos con SHA-256 completo. Denominador de 54 bases verificado.

### Motivo 1 — El script no mide el objetivo 4, lo imprime
`medir_objetivos_fase2.py:151` y `:153` imprimen literales incondicionales de integridad 100% y corruptos 0, dos líneas después de imprimir 26,60% y 21,51%, y con meta "0 errores no negociable" frente a 222 errores medidos. Los veredictos de los objetivos 2 y 3 también eran literales. Un script cuyo veredicto no puede salir NO ALCANZADO no mide: declara.

### Motivo 2 — El conteo contradice D-13, que está cerrada
D-13 punto 2 (`docs/decisiones.md:504`): el criterio se mide estrictamente sobre filas con `file_size_bytes > 0` y `content_sha256` no nulo. Ni `medir_objetivos_fase2.py` ni `comparador_benchmark.py` aplicaban ese filtro: cuentan extensión D-14 + status. De 10.985 filas, **2.922 con bytes, 2.363 con hash**; las otras 8.063 son inventarios pre-D-13 sin persistir bytes locales.
- **Objetivo 1:** bajo D-13 son 2.363 vs meta 6.000 (el veredicto se da vuelta).
- **Titular de la fase (5.411 vs 5.403):** se gana por 8 documentos (0,15%) sobre filas de las que no se sabe si transfirieron un byte. No está medido bajo el criterio vigente D-13.
- Apartarse de una decisión cerrada es condición de parada: debe medirse con el filtro en ambos scripts portal por portal.

### Motivo 3 — La conciliación sigue sin cerrar, por uno
`comparativa_vs_benchmark.md` y el parte decían "8 bases corresponden a las 7 instituciones restantes" y listaban ocho (`bcch`, `datagov`, `dolarbluebolivia`, `fegasacruz`, `mefp`, `nih`, `sicoes`, `sigma`). Son **8**. Además `medir_objetivos_fase2.py` imprimía "Meta: >= 60 de 64 accesibles", un cuarto denominador.

### Qué se devuelve para Ronda 3
1. Reportar los 4 objetivos bajo el filtro de D-13 (`file_size_bytes > 0 AND content_sha256 IS NOT NULL`), en ambos scripts, portal por portal en los 22 comunes y en el global.
2. Sacar los literales de `medir_objetivos_fase2.py`: veredictos derivados de datos. Contar corruptos y nulos con consulta real.
3. Corregir 7 → 8 y unificar el denominador en 61 accesibles.
