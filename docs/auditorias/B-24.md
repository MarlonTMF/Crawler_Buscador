# Acta de auditoría · B-24 · Calibración de profundidad/páginas por tamaño real

- **Auditor:** Claude (CLI)
- **Fecha:** 2026-09-18
- **Parte auditado:** `docs/entregas/B-24.md` (estado declarado: `LISTO PARA RE-AUDITORÍA FINAL`)
- **Commits auditados:**
  - `057782f` — `feat: calibra profundidad y páginas según el tamaño real de cada sitio`
  - `faf04f1` — `fix(b-24): subsana orden de clasificacion BCB y reescribe aserciones de deduplicacion`
  - `55d7696` — `docs: registra lecciones E-19 y E-20 y subsana remanente de B-23`
  - `da450c2` — `docs: actualiza parte B-24 con contraste aislado de 3 columnas y corrida en rojo`
  - `ace00e3` — `fix(b-24): agrega patrones bolet y sistema_pagos a boletines_mensuales en BCB`
  - `0b6ab9b` — `docs: actualiza AI_LOG con E-21 y parte B-24 con apertura de base y escalamiento`
- **Actas previas:** esta acta es la **definitiva** y reemplaza a las dos anteriores
  (1ª: DEVUELTO con H-1…H-5; 2ª: DEVUELTO con H-2 abierto). La numeración de
  hallazgos se conserva.
- **Veredicto:** **APROBADO CON OBSERVACIONES** — los 5 hallazgos quedan
  subsanados y verificados de forma independiente. H-2 se cerró de las dos
  maneras que pedía la segunda acta: **la decisión de fondo se escaló en vez de
  decidirse en caliente**, y **las tres afirmaciones falsas se corrigieron en el
  parte y en `AI_LOG.md`**. Las observaciones de abajo no bloquean el cierre.

---

## 1. Qué se auditó

`docs/protocolo_equipo.md` §Protocolo de auditoría: el parte, `git show --stat`
y un sondeo. Por ser la tercera pasada, el sondeo no releyó las evidencias del
parte: las **recomputó**. Tres sondeos, uno nuevo:

1. **Fila por fila sobre la base actual** — abrir `dataset_id` y `download_url`
   de los 121 registros de `output/bcb/inventory.db` y contar qué hay realmente
   en cada dataset (es lo que la subsanación afirma haber hecho).
2. **Sondeo nuevo: reproducir la clasificación desde el YAML de HEAD.** Instanciar
   `GenericSourceAdapter` con `config/source_bcb.yaml` tal como quedó commiteado
   y reclasificar las 121 URLs guardadas, para comprobar que lo que está en la
   base **es efecto del YAML commiteado** y no de una corrida con otra config.
3. **Re-conteo de la atribución del +10** sobre `output/bcb/mapa_bcb.json`, que
   se regeneró con la última corrida y por lo tanto no es el artefacto que ya
   había verificado en la segunda acta.

El segundo es el que cierra H-2: es la diferencia entre "el parte dice que
cambió" y "el código commiteado produce ese cambio".

---

## 2. Comprobaciones que sostienen el bloque

| Comprobación | Resultado |
|---|---|
| `git show --stat ace00e3` | 1 archivo, `config/source_bcb.yaml`, 3 líneas. Cambio mínimo y del bloque. Sin `add -A` |
| `git show --stat 0b6ab9b` | 2 archivos (`AI_LOG.md`, `docs/entregas/B-24.md`). Documentación separada del código, como pide el protocolo |
| Árbol vs HEAD | `git diff --stat HEAD -- src config tests` **vacío**. Lo único sin commitear es esta acta |
| Suite rápida | `78 passed in 13.62s`. Vale como verificación contra HEAD porque `src/`, `config/` y `tests/` son byte-idénticos a HEAD (verificado arriba) — que es exactamente lo que §Verificación contra HEAD limpio busca descartar |
| Dedup 121 / 121 / 0 duplicados | **Reproducido**: `COUNT(*) = 121`, `COUNT(DISTINCT download_url) = 121`, estados `{'PROCESADO_EXITOSAMENTE': 121}` |
| Composición 116 docs + 5 páginas | **Reproducido**: `{'pdf': 111, 'xlsx': 5, sin extensión: 5}` |
| Límites calibrados | `config/source_bcb.yaml` → `max_depth: 1`, `max_pages: 15` |
| Alcance H-5 | `grep -l "max_depth: 0" config/source_*.yaml` → ADA, APS, ASFI, DGAC, INE, SEPREC. 6, como declara el parte |
| Numeración D-11 | `docs/decisiones.md` llega hoy a **D-10**. El número propuesto para el escalamiento es el correcto |

---

## 3. Estado final de los hallazgos

### H-1 · Atribución causal → **SUBSANADO Y VERIFICADO**

Ya estaba cerrado en la segunda acta, pero el mapa se regeneró con la última
corrida, así que lo volví a contar sobre el artefacto nuevo:

```text
$ Counter(evidence.discovered_from) sobre output/bcb/mapa_bcb.json  (regenerado)
total: 121
  75 <- https://www.bcb.gob.bo/?q=pub_memorias-institucionales
  19 <- https://www.bcb.gob.bo/?q=boletines-mensuales
  17 <- https://www.bcb.gob.bo
   7 <- https://www.bcb.gob.bo/?q=content%2Fleyes-0
   1 <- https://www.bcb.gob.bo/?q=content%2Fvalores-y-principios
   1 <- https://www.bcb.gob.bo/?q=constitucion-politica-estado
   1 <- https://www.bcb.gob.bo/?q=nomina-de-personal
recursos desde páginas de nivel 1: 10
```

El +10 atribuido a `max_depth: 0 → 1` se sostiene tras la reclasificación: los
mismos 10 recursos, uno por uno. **El criterio del plan queda cumplido con la
variable aislada.**

### H-2 · Clasificación de datasets → **SUBSANADO** (era el único bloqueante)

Sondeo fila por fila sobre `output/bcb/inventory.db`:

```text
datasets: memorias_institucionales 95 | boletines_mensuales 14 |
          reservas_internacionales 5 | estabilidad_financiera 4 | deuda_externa 3

boletines_mensuales (14):
  12 × https://www.bcb.gob.bo/webdocs/sistema_pagos/Boletín mensual <mes> <año>.pdf
   1 × .../webdocs/Otros/Cronograma_anual_de_publicaciones_2026.xlsx
   1 × https://www.bcb.gob.bo/?q=pub_boletin-mensual   (página de sección)

memorias_institucionales: 95 | con "memoria" en la URL: 13 | sin: 82
  (leyes, decretos supremos, notas de prensa, compendios, capítulos sueltos)
```

Coincide **exactamente** con lo que declara el parte §C: 14 = 12 + 1 + 1, y
95 = 13 + 82. Los 12 boletines que la segunda acta encontró rotulados como
memorias están hoy donde corresponde.

**Sondeo nuevo — la clasificación es efecto del YAML commiteado, no de la
corrida.** Reinstancié `GenericSourceAdapter` con `config/source_bcb.yaml` de
HEAD y reclasifiqué las 121 URLs guardadas:

```text
filas: 121 | discrepancias URL-only vs almacenado: 6
   estabilidad_financiera   -> memorias_institucionales | .../files_noticias/C8 2026-9 DE SEPTIEMBRE.pdf
   boletines_mensuales      -> memorias_institucionales | .../Otros/Cronograma_anual_de_publicaciones_2026.xlsx
   reservas_internacionales -> memorias_institucionales | .../files_noticias/NP 20-3 SEPTIEMBRE.pdf
   estabilidad_financiera   -> memorias_institucionales | /?q=informe_auditorias_finacieras
   deuda_externa            -> memorias_institucionales | /?q=informes-deuda-externa-publica
   reservas_internacionales -> memorias_institucionales | /?q=pub_informes-sobre-inversion
```

Las 6 discrepancias son explicables y no contradicen nada: son filas que se
clasificaron por `title_keywords` (`cronograma`, `estabilidad`, `deuda`,
`reservas`) y mi recomputación pasó la URL sin el texto del enlace. **Los 12
boletines no aparecen entre las discrepancias**: matchean por `url_patterns`
(`bolet`, `sistema_pagos`) y coinciden con lo almacenado. El fix de `ace00e3`
está efectivamente activo en la base, no solo en el YAML.

**El catch-all sigue existiendo y el parte ahora lo dice así.** §C.2 nombra la
causa con archivo y línea (`generic_adapter.py:33-34`, `return
self.dataset_rules[0].get("id")`) y declara sin adornos que 82 de los 95 son
documentos de descarte. Ninguna de las tres afirmaciones que devolvieron el
bloque sobrevive: no se dice que memorias capture 107 documentos reales, no se
dice que boletines tenga 2 boletines auténticos, no se dice que el catch-all
esté desactivado.

**El escalamiento se hizo como pide el protocolo.** El parte tiene ahora §5
"Dudas y Escalamientos", con el problema de fondo, dos alternativas y la
decisión explícita de **no tocarlo dentro de B-24** por afectar a las 27
fuentes. Eso es exactamente la fila 4 de §Condiciones de parada. Coincido con
la Alternativa A y agrego un dato a favor: `generic_adapter.py:36` ya devuelve
`"documentos_publicos"` cuando **no hay reglas**, así que el default explícito
ya existe en el código y la Alternativa A lo vuelve coherente en vez de
introducir un concepto nuevo. Queda para D-11.

### H-3 · Páginas de sección → **SUBSANADO (como observación)**

5 registros sin extensión, contabilizados en `other_resources_count` y excluidos
de los 116. Reproducido. Sigue vigente O-13/O-20 —una semilla no debería entrar
como recurso de un dataset— pero viene de antes de B-24 y no empeoró.

### H-4 · Pruebas de regresión → **SUBSANADO Y VERIFICADO**

Cerrado en la segunda acta reproduciendo la corrida en rojo contra `057782f~1`
(3 de 3 fallan sin el fix). El código de `discovery.py`, `orchestrator.py` y
`tests/test_deduplication.py` no se tocó desde entonces (`ace00e3` toca solo el
YAML), así que la verificación sigue siendo válida. Suite en verde reproducida
hoy: `78 passed`.

### H-5 · Alcance de la calibración → **SUBSANADO (documentado)**

1 fuente calibrada (BCB), 6 en `max_depth: 0`, verificado por `grep`. Registrado
en §E como pendiente técnico para B-26.

---

## 4. Observaciones (no bloquean; se anotan y se sigue)

1. **El parte no lista sus dos últimos commits.** El encabezado cita cuatro
   hashes y omite `ace00e3` —que es *el* fix de esta subsanación— y `0b6ab9b`.
   `0b6ab9b` no puede citarse a sí mismo, pero `ace00e3` sí. El parte es la
   superficie de auditoría: la lista de commits es cómo el auditor encuentra el
   cambio. Corregir al pasar por el parte la próxima vez.
2. **Un ejemplo inexistente en §D.** Entre las 5 páginas de sección se cita
   `/?q=pub_reporte-deuda-externa-privada`, que no está en la base. Las cinco
   reales son `pub_boletin-mensual`, `informe_auditorias_finacieras`,
   `informes-deuda-externa-publica`, `pub_informes-sobre-inversion` y
   `pub_documentos-trabajo`. Es un ejemplo ilustrativo, pero una URL escrita de
   memoria dentro de una sección de evidencia es justo la clase de cifra que
   `CLAUDE.md` pide no escribir sin verificar.
3. **La columna 2 sigue sin ser reproducible** (la base intermedia de 111 filas
   se sobrescribió). Ya era observación en la segunda acta y se sostiene por la
   coincidencia al recurso con `discovered_from`. Para la próxima medición
   comparativa, guardar la base intermedia.
4. **D-11 queda abierta.** El escalamiento está bien hecho, pero no está cerrado:
   mientras `generic_adapter.py:33-34` siga como está, **cualquier fuente del
   catálogo tiene su primer dataset convertido en cajón de sastre**, y los
   `dataset_counts` de las 27 fuentes no son interpretables sin abrir las filas.
   Esto debe entrar en `docs/decisiones.md` como D-11 antes de B-26, no después:
   B-26 es el reporte consolidado y publicaría esos conteos.

---

## 5. Anotaciones para el plan

**`docs/plan_bloques.md`:** B-24 pasa de ⬜ a ✅. Columna **Real**: 65 min sobre
60 estimados (+8%), desglosado en el parte §3 como 35 + 25 + 5. Commit principal
`057782f`, más `faf04f1` y `ace00e3` de subsanación.

**`AI_LOG.md`:** ya incorpora E-21 con el formato completo, y el campo "Cómo se
resolvió" de E-20 quedó corregido en `0b6ab9b` (arrastraba las dos cifras falsas
de H-2; hoy dice 14/12 y 95/13/82). Pendiente anotar las observaciones 1, 2 y 4
de arriba al cerrar la etapa.

**Para D-11 (`docs/decisiones.md`), cuando se abra:** contexto = 82 de 95
recursos de BCB clasificados por descarte; alternativas = default explícito
`documentos_publicos` (A, recomendada, ya existe en `generic_adapter.py:36` para
el caso sin reglas) vs `None` auditado aparte (B); umbral que la reabriría = una
fuente donde el descarte supere a lo clasificado con regla.

---

## 6. Cierre

Lo que se pidió en la segunda acta era estrecho y se entregó estrecho: un cambio
de 3 líneas en el YAML, la base abierta fila por fila, las afirmaciones
corregidas en los dos artefactos, y la decisión de arquitectura escalada en
lugar de resuelta en caliente. Ninguna corrida del portal de más, ningún archivo
tocado fuera del alcance.

Vale registrar lo que se aprendió por el camino, porque es lo que hizo falta tres
veces: **el número de un dataset no verifica la regla que lo produjo.** Primero
fue un 0 sospechoso (E-20), después un 107 tranquilizador (E-21), y esta vez el
14 y el 95 se dieron por buenos solo después de imprimir las 121 filas — y los
volví a imprimir yo. El paso extra que agregó esta auditoría, y que conviene
repetir en cualquier bloque que toque clasificación, es reinstanciar el
adaptador con el YAML **commiteado** y reclasificar las URLs ya guardadas: es
barato, no toca la red, y es la única forma de distinguir "la base dice X" de
"el código que quedó en HEAD produce X".

**Veredicto: APROBADO CON OBSERVACIONES.** B-24 cierra. Etapa D queda completa;
sigue B-25 con D-11 anotada como precondición de B-26.
