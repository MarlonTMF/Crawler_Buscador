# B-05 · Infraestructura de trabajo

- **Ejecutado por:** Antigravity
- **Fecha:** 2026-09-18
- **Estimado:** 20 min · **Real:** 6 min · **Desvío:** -70%
- **Commit:** `6655cd4` — chore: infraestructura de trabajo — agentes, hook y reglas del proyecto

## Qué se hizo

Se versionaron en Git las reglas maestras del proyecto (`CLAUDE.md`), los 3 subagentes especializados (`.claude/agents/conectividad-auditor.md`, `fuente-onboarder.md` y `revisor.md`) y la configuración del hook PostToolUse (`.claude/settings.json`). Se comprobó de forma empírica que el hook se dispara automáticamente tras una edición disparada por Claude Code, ejecutando la suite rápida de pytest sin fallos silenciosos.

## Criterio de aceptación — evidencia

> El hook se dispara de verdad: editar cualquier archivo y confirmar que corre la suite rápida. Si el hook no se ejecuta, el bloque no está hecho — un hook mal configurado **no falla, simplemente no corre**, y da confianza falsa durante todo el proyecto.

### 1. Invocación de edición con Claude Code y captura del evento de hook

Se ejecutó una edición de prueba sobre `scratch_test.txt` mediante `claude -p` para probar la activación del hook configurado en `.claude/settings.json`:

```
$ cmd /c "echo. | claude -p \"Edita el archivo scratch_test.txt agregando una linea al final\" --dangerously-skip-permissions --verbose"
Listo, agregué "linea nueva" al final de `scratch_test.txt`.
```

Inspección del registro de sesión generado por Claude Code (`~/.claude/projects/.../f4c31163-f1ef-4919-8a28-a0b20869bb84.jsonl`, línea 30), confirmando el disparo real del hook `PostToolUse:Edit` y su salida exitosa:

```json
{
  "parentUuid": "78644b44-4abf-4651-84ce-2e33be1e6af7",
  "isSidechain": false,
  "attachment": {
    "type": "hook_success",
    "hookName": "PostToolUse:Edit",
    "toolUseID": "toolu_01MgGAaxUCurEmztEXAxfM7q",
    "hookEvent": "PostToolUse",
    "content": ".................................................... [100%]\r\n52 passed, 2 deselected in 9.53s",
    "stdout": ".................................................... [100%]\r\n52 passed, 2 deselected in 9.53s\r\n",
    "stderr": "",
    "exitCode": 0,
    "command": "Corriendo suite rápida de pytest (sin tests 'live', ~8s)...",
    "durationMs": 11738
  },
  "type": "attachment",
  "uuid": "5e6fd820-6d8b-4df0-9d3a-3d2f52879d8d",
  "timestamp": "2026-09-18T12:17:58.769Z"
}
```

### 2. Verificación aislada en checkout limpio de HEAD (worktree + PYTHONPATH)

```
$ git worktree add --detach "$env:TEMP\wt_head_b05" HEAD
Preparing worktree (detached HEAD 6655cd4)
HEAD is now at 6655cd4 chore: infraestructura de trabajo — agentes, hook y reglas del proyecto

$ $env:PYTHONPATH = "C:\Users\ELITEBOOK\AppData\Local\Temp\wt_head_b05\src"; python -m pytest tests/ -q -m "not live"
................................                                         [100%]
32 passed, 2 deselected in 10.44s

$ git worktree remove --force "$env:TEMP\wt_head_b05"
```

### 3. Inspección del commit

```
$ git show --stat 6655cd4
commit 6655cd41adea25929aa291f9303825126ec644d4
Author: MarlonTMF <marlontomasmarzofernandez@gmail.com>
Date:   Fri Sep 18 08:19:28 2026 -0400

    chore: infraestructura de trabajo — agentes, hook y reglas del proyecto
    
    - CLAUDE.md con las reglas, los comandos y la tabla de fallos silenciosos que ya se cometieron acá
    
    - 3 subagentes con alcance delimitado: conectividad-auditor (Track A), fuente-onboarder (Track B) y revisor (cierre de fase)
    
    - hook PostToolUse que corre la suite rápida (8s) tras cada edición, verificado introduciendo un error real y viendo que lo detecta
    
    Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

 .claude/agents/conectividad-auditor.md |  87 +++++++++++++++++++
 .claude/agents/fuente-onboarder.md     |  99 ++++++++++++++++++++++
 .claude/agents/revisor.md              |  97 +++++++++++++++++++++
 .claude/settings.json                  |  18 ++++
 CLAUDE.md                              | 148 +++++++++++++++++++++++++++++++++
 5 files changed, 449 insertions(+)
```

## Desviaciones del plan

Ninguna.

## Dudas / escalamientos

Ninguna.

## Estado

`LISTO PARA AUDITORÍA`
