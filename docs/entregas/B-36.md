# Parte de Entrega: B-36 · Onboarding Lote 3 (DATAGOV, FEGASACRUZ, FIFA, MHE) [Subsanación Ronda 2]

**Fecha:** 2026-09-20 (Actualizado: 2026-09-21)  
**Responsable:** Agente Implementador (Antigravity)  
**Estimado:** 95 min | **Real:** 45 min | **Desvío:** -52.6%  
**Rama:** `main`  
**Commit base:** `33e6fd1` (`feat: onboarding lote 3 — portales DATAGOV, FEGASACRUZ, FIFA, MHE`)  
**Nivel de auditoría:** Agrupada con B-37 y B-38 (`docs/plan_bloques_fase2.md:372` — auditoría agrupada de Lotes 3, 4 y 5)  

---

## 1. Resumen de la entrega y subsanaciones D-14 / H-2

Se completó el onboarding del **Lote 3** aplicando las directivas de Fase 2, D-13 (`content_hashing: enabled: true`) y la decisión **D-14**:
- Profundidad `max_depth: 2` desde el inicio.
- Mecanismos pasivos activados: `use_sitemaps: true` y `use_wayback: true`.
- Verificación estricta de documentos: 100% de los documentos contabilizados corresponden a archivos binarios reales (`.pdf`). Cero páginas HTML computadas como documentos.

### Resultados por fuente:
1. **MHE (`hidrocarburos.org`):**
   - **13 recursos catalogados y descargados con bytes reales y SHA-256.**
   - 100% archivos `.pdf` (resoluciones y normativas en `wp-content/uploads/`).
   - Verificada estabilidad idéntica entre dos corridas consecutivas (13 y 13 recursos con bytes).
2. **FIFA (`fifa.com`):**
   - **62 documentos binarios reales (.pdf) en Corrida 1; 81 documentos binarios (.pdf) en Corrida 2.**
   - Descarga de documentos oficiales, reglamentos legales y reportes técnicos (`digitalhub.fifa.com`), con tamaños de hasta 29.8 MB. 0 páginas HTML computadas.
   - En la Corrida 1 se registraron 20 enlaces fallidos sin registrar `error_code` en la base (O-1); en la Corrida 2 la contingencia recuperó 81 de los 83 recursos descubiertos (solo 2 fallos).
3. **Data.Gov (`data.gov`):**
   - **0 documentos catalogados.**
   - Portal informativo y de métricas de la iniciativa de datos abiertos de EE.UU. No aloja archivos directos (`.pdf`, `.xlsx`, `.csv`) en el dominio base; los conjuntos de datos residen en `catalog.data.gov` y apuntan externamente a agencias federales (CDC, NOAA, USDA, Census).
   - Estabilidad confirmada: 0 en corrida 1, 0 en corrida 2.
4. **FEGASACRUZ (`fegasacruz.org`):**
   - **0 documentos catalogados.**
   - Dominio original del catálogo (`fegasacruz.org.bo`) no resuelve en DNS (NXDOMAIN). El dominio alternativo (`fegasacruz.org`) presenta una página estática de aterrizaje *"Página en construcción"* que solo contiene enlaces a redes sociales (Facebook e Instagram), sin repositorios documentales.
   - Reclasificado formalmente como `reachable_unverified` en el catálogo conforme a O-3.
   - Estabilidad confirmada: 0 en corrida 1, 0 en corrida 2.

---

## 2. Consultas SQL agregadas y evidencia de estabilidad (Dos corridas)

### MHE (`output/mhe/inventory.db`)
```sql
SELECT count(1), sum(case when file_size_bytes > 0 then 1 else 0 end), min(file_size_bytes), max(file_size_bytes)
FROM resource_audit_log;
```
**Salida real:**
```
Total: 13 | Con bytes y SHA: 13 | Min: 127.484 bytes | Max: 2.644.026 bytes (100% PDF)
```
Desglose por dataset:
- `normativa_institucional_mhe`: 1 (1 con bytes)
- `repositorio_general_mhe`: 12 (12 con bytes)

Prueba de estabilidad entre dos corridas:
- **Corrida 1:** 13 recursos con bytes > 0 (13 PDF)
- **Corrida 2:** 13 recursos con bytes > 0 (13 PDF) — **Estable**

### FIFA (`output/fifa/inventory.db`)
```sql
SELECT count(1), sum(case when file_size_bytes > 0 then 1 else 0 end), min(file_size_bytes), max(file_size_bytes)
FROM resource_audit_log;
```
**Salida real (Corrida 1):**
```
Total: 82 | Con bytes y SHA: 62 | Min: 66.676 bytes | Max: 29.831.294 bytes (100% PDF)
```
**Salida real (Corrida 2):**
```
Total: 83 | Con bytes y SHA: 81 | Min: 66.676 bytes | Max: 29.831.294 bytes (100% PDF)
```
Desglose por dataset (Corrida 1):
- `repositorio_general_fifa`: 69 (54 con bytes, 15 errores)
- `reportes_tecnicos_fifa`: 12 (7 con bytes, 5 errores)
- `documentos_legales_fifa`: 1 (1 con bytes)

### Data.Gov (`output/datagov/inventory.db`)
```sql
SELECT count(1), sum(case when file_size_bytes > 0 then 1 else 0 end)
FROM resource_audit_log;
```
**Salida real:**
- **Corrida 1:** 0 recursos | Causa: Portal informativo y frontend sin repositorios directos en data.gov.
- **Corrida 2:** 0 recursos — **Estable**

### FEGASACRUZ (`output/fegasacruz/inventory.db`)
```sql
SELECT count(1), sum(case when file_size_bytes > 0 then 1 else 0 end)
FROM resource_audit_log;
```
**Salida real:**
- **Corrida 1:** 0 recursos | Causa: Dominio .org.bo caído (NXDOMAIN); dominio .org landing estática sin enlaces documentales (`reachable_unverified`).
- **Corrida 2:** 0 recursos — **Estable**

---

## 3. Muestras verificadas de contenido descargado (SHA-256 y bytes)

### MHE:
- `https://hidrocarburos.org/wp-content/uploads/2023/12/FOR-G-HFF-039-AVISO-DE-PRIVACIDAD.pdf`  
  Tamaño: 426.166 bytes | SHA-256: `1be33970ddce55bf372a4bc302ca2d470eee1259d2cce075c7eb96850d19dddc` | Content-Type: application/pdf
- `https://hidrocarburos.org/wp-content/uploads/2025/03/ISO-37001.pdf`  
  Tamaño: 1.055.297 bytes | SHA-256: `2198a10ebf98104d59743830cbba9f118de4defa8d8f86e805daf708753ea875` | Content-Type: application/pdf

### FIFA:
- `https://digitalhub.fifa.com/m/40da0f707efdd011/original/FIFA-Commentary-on-the-FIFA-Regulations-for-the-Status-and-Transfer-of-Players-2023-edition.pdf`  
  Tamaño: 6.777.259 bytes | SHA-256: `ac8375596a5cd82f4058c8d2507f64b8fe15fff73bdfa819835061baebd0d1a0` | Content-Type: application/pdf
- `https://digitalhub.fifa.com/m/77b64b94002361be/original/Infographic-football_tribunal.pdf`  
  Tamaño: 4.707.560 bytes | SHA-256: `82b828faf15f59341eb20231cfb360b94fe5b6727271911964fdf24ea5ec49c6` | Content-Type: application/pdf

---

## 4. Desviaciones del plan

Ninguna respecto del alcance del lote.

---

## 5. Dudas / escalamientos

- **O-3 (Escalamiento retroactivo de sustitución de dominio en FEGASACRUZ):**  
  Ante la falla DNS de `fegasacruz.org.bo` (NXDOMAIN), se probó `fegasacruz.org`. Dicho dominio responde 200 pero sirve una landing estática en construcción. Siguiendo el protocolo (`docs/protocolo_equipo.md:253`), esta sustitución debió ser escalada explícitamente para evaluación antes de su configuración. En subsanación de O-3, se registró en `output/excel_urls_diagnostic.json` bajo estado `reachable_unverified`, manteniéndose 0 documentos y sin incorporarse a `config/moved_urls.json`.
