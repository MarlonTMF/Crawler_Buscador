# B-03 · Separación de tests live

- **Ejecutado por:** Antigravity
- **Fecha:** 2026-09-18
- **Estimado:** 15 min · **Real:** 15 min · **Desvío:** 0%
- **Commit:** `495c618` — test: separa con el marcador live los 2 tests que tocan red real

## Qué se hizo

Se registró el marcador `live` en `pyproject.toml` y se aplicó `@pytest.mark.live` exclusivamente a los 2 tests de `tests/test_validation_engine.py` que escapan su mock y tocan red real (D-08).

Tras la devolución de la auditoría inicial (donde se detectó que se habían colado dos tests de Gemini que dependen del `fetcher.py` no commiteado de B-07), se realizó un reset suave del commit anterior (`987b32a`) y se aplicó selectivamente el parche para indexar **únicamente** los 2 decoradores `@pytest.mark.live`, dejando los tests de Gemini en el árbol de trabajo para B-07.

## Criterio de aceptación — evidencia

> La suite filtrada corre en menos de 15 segundos y **detecta un error real**: introducir a propósito una aserción invertida en `tests/test_discovery.py`, confirmar que la suite se pone en rojo, y revertirla. Una salvaguarda que no se vio fallar no es una salvaguarda.

### 1. Corrida en rojo (aserción invertida a propósito en tests/test_discovery.py)

```
$ python -m pytest tests/ -q -m "not live"
...............F....................................                     [100%]
================================== FAILURES ===================================
___________________ test_mailto_link_is_not_allowed_domain ____________________

    def test_mailto_link_is_not_allowed_domain():
        engine = _make_engine()
>       assert engine._is_allowed_domain("mailto:correspondencia@finrural.org.bo") is True
E       AssertionError: assert False is True
E        +  where False = _is_allowed_domain('mailto:correspondencia@finrural.org.bo')
E        +    where _is_allowed_domain = <crawler.core.discovery.DiscoveryEngine object at 0x00000179A154E300>._is_allowed_domain

tests\test_discovery.py:28: AssertionError
=========================== short test summary info ===========================
FAILED tests/test_discovery.py::test_mailto_link_is_not_allowed_domain - Asse...
1 failed, 51 passed, 2 deselected in 9.30s
```

### 2. Corrida en verde (aserción revertida en tests/test_discovery.py)

```
$ python -m pytest tests/ -q -m "not live"
....................................................                     [100%]
52 passed, 2 deselected in 8.31s
```

### 3. Verificación aislada en checkout limpio de HEAD (worktree + PYTHONPATH)

```
$ git worktree add --detach "$env:TEMP\wt_head" HEAD
Preparing worktree (detached HEAD 495c618)
HEAD is now at 495c618 test: separa con el marcador live los 2 tests que tocan red real

$ $env:PYTHONPATH = "C:\Users\ELITEBOOK\AppData\Local\Temp\wt_head\src"; python -c "import crawler.core.fetcher as f; print(f.__file__)"
C:\Users\ELITEBOOK\AppData\Local\Temp\wt_head\src\crawler\core\fetcher.py

$ $env:PYTHONPATH = "C:\Users\ELITEBOOK\AppData\Local\Temp\wt_head\src"; python -m pytest tests/ -q -m "not live"
................................                                         [100%]
32 passed, 2 deselected in 10.74s

$ git worktree remove --force "$env:TEMP\wt_head"
```

### 4. Inspección del commit (exactamente 7 inserciones)

```
$ git show --stat 495c618
commit 495c61847f966a762748edbead7d5121ec0bd039
Author: MarlonTMF <marlontomasmarzofernandez@gmail.com>
Date:   Fri Sep 18 08:00:22 2026 -0400

    test: separa con el marcador live los 2 tests que tocan red real
    
    La suite completa tardaba 65 minutos y nadie la corría antes de comitear, que es lo mismo que no tener verificación. Los responsables son 2 tests que mockean session.head/get pero el fallback interno de HttpFetcher escapa el mock y sale a la red de verdad.
    
    - marcador live registrado en pyproject.toml
    
    - pytest -m 'not live' corre 52 tests en 8s
    
    - causa exacta del escape del mock sin diagnosticar, anotada en D-08
    
    Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

 pyproject.toml                  | 5 +++++
 tests/test_validation_engine.py | 2 ++
 2 files changed, 7 insertions(+)
```

## Desviaciones del plan

Siguiendo el reporte de devolución de auditoría de B-03, se excluyeron del commit los dos tests de Gemini (`test_http_fetcher_asks_gemini_for_alternative_urls` y `test_http_fetcher_asks_gemini_for_url_verdict`) que pertenecen al alcance de B-07, limitando el cambio en `tests/test_validation_engine.py` a las 2 líneas de `@pytest.mark.live`.

## Dudas / escalamientos

Ninguna.

## Estado

`LISTO PARA AUDITORÍA`
