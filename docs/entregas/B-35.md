# Parte de Entrega: B-35 · Onboarding Lote 2 (CEPROBOL, BCB_BRASIL, BCCH, DOLARBLUEBOLIVIA)

**Fecha:** 2026-09-20  
**Responsable:** Agente Implementador (Antigravity)  
**Estimado:** 95 min | **Real:** 55 min | **Desvío:** -42.1%  
**Rama:** `main`  
**Commit:** `feat: onboarding lote 2 — portales CEPROBOL, BCB_BRASIL, BCCH, DOLARBLUEBOLIVIA`  
**Nivel de auditoría:** Agrupada con B-34 (`docs/plan_bloques_fase2.md:372`)  

---

## 1. Resumen de la entrega

Se completó el onboarding del **Lote 2** de portales pendientes de cobertura, aplicando las correcciones de la Fase 2:
- Profundidad `max_depth: 2` desde el inicio.
- Mecanismos pasivos activados: `use_sitemaps: true` y `use_wayback: true`.
- Descarga real y registro de bytes habilitado: `content_hashing: enabled: true`.
- Para sitios SPA (Single-Page Apps en JavaScript): renderizado con `use_playwright: true` (BCB_BRASIL).

### Resultados por fuente:
1. **CEPROBOL (`probolivia.gob.bo`):**
   - **54 recursos catalogados, 53 descargados con bytes reales y SHA-256.**
   - Descubrimiento de sitemap WordPress (`wp-sitemap.xml`) y enlace profundo a Rendición Pública de Cuentas y normativas.
2. **BCB_BRASIL (`bcb.gov.br`):**
   - **27 recursos catalogados, 26 descargados con bytes reales y SHA-256.**
   - Banco Central do Brasil es una SPA en Angular (`<app-root></app-root>`); la activación de Playwright headless permitió renderizar el DOM y extraer manuales, publicaciones CADIP e informes históricos (hasta 23.7 MB).
3. **BCCH (`bcentral.cl`):**
   - **0 documentos catalogados.**
   - El portal está protegido por **Imperva Incapsula WAF** (`/_Incapsula_Resource` iframe challenge con directiva `NOINDEX, NOFOLLOW` para scraping automatizado). El servidor CDX de Wayback Machine presentó intermitencia/timeout 503 durante la prospección pasiva. Se documenta fehacientemente la causa técnica de la ausencia de documentos.
4. **DOLARBLUEBOLIVIA (`dolarbluebolivia.click`):**
   - **0 documentos catalogados.**
   - Portal web de cotización informal de divisas en tiempo real con sitemap de 2 URLs HTML (`/`, `/about/`). No posee repositorio de documentos descargables (`.pdf`, `.xlsx`, `.csv`).

---

## 2. Consultas SQL agregadas y salidas reales

### CEPROBOL (`output/ceprobol/inventory.db`)
```sql
SELECT count(1), sum(case when file_size_bytes > 0 then 1 else 0 end), min(file_size_bytes), max(file_size_bytes)
FROM resource_audit_log;
```
**Salida real:**
```
Total: 54 | Con bytes y SHA: 53 | Min: 57.766 bytes | Max: 6.840.304 bytes
```
Desglose por dataset:
- `repositorio_general_probolivia`: 45
- `publicaciones_normativa_probolivia`: 7
- `rendicion_cuentas_probolivia`: 2

### BCB_BRASIL (`output/bcb_brasil/inventory.db`)
```sql
SELECT count(1), sum(case when file_size_bytes > 0 then 1 else 0 end), min(file_size_bytes), max(file_size_bytes)
FROM resource_audit_log;
```
**Salida real:**
```
Total: 27 | Con bytes y SHA: 26 | Min: 2.871 bytes | Max: 23.708.453 bytes
```
Desglose por dataset:
- `publicaciones_cadip_bcb`: 24
- `relatorios_estatisticas_bcb`: 2
- `repositorio_general_bcb`: 1

### BCCH (`output/bcch/inventory.db`)
```
Total: 0 | Causa: WAF Imperva Incapsula challenge (status 200 con iframe challenge).
```

### DOLARBLUEBOLIVIA (`output/dolarbluebolivia/inventory.db`)
```
Total: 0 | Causa: Sitio dashboard web de divisas en vivo sin repositorio documental.
```

---

## 3. Muestras literales y verificación de primeros bytes

### Muestra 1: Normativa Pro-Bolivia (PDF)
- **URL:** `https://www.probolivia.gob.bo/wp-content/uploads/2021/05/norma-23-ley2471.pdf`
- **Dataset:** `publicaciones_normativa_probolivia`
- **Tamaño:** 57.952 bytes | **SHA256:** `9e696aa399c5e4a4e7fbf374ea6b81ab589e42fe59a1d9b571ef157863cc837d`
- **Magic bytes:** `b'%PDF-1.3'`

### Muestra 2: Historia Contada BCB Volumen 25 (PDF)
- **URL:** `https://www.bcb.gov.br/historiacontada/publicacoes/hc_bc_volume_25_henrique_meirelles.pdf`
- **Dataset:** `publicaciones_cadip_bcb`
- **Tamaño:** 23.708.453 bytes (23.7 MB) | **SHA256:** `7a18f154ff661eb0d0c57fff7d11090f4642c342a20200ca2f008dbca1791eb5`
- **Magic bytes:** `b'%PDF-1.7'`

---

## 4. Estado de la Suite de Pruebas
```
$ python -m pytest tests/ -q -m "not live"
104 passed in 14.16s
```

---

## 5. Desviaciones del plan
Ninguna. Se ejecutó la calibración a profundidad 2 con sitemaps y wayback.

---

## 6. Dudas / escalamientos
Ninguna.

---

## 7. Estado
**ENTREGADO** — Procede a la primera auditoría agrupada (B-34 + B-35) según `docs/plan_bloques_fase2.md:372`.
