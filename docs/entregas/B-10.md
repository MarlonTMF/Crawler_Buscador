# B-10 · SICSANTACRUZ: Resolución tras expiración de dominio y canal sucesor

- **Ejecutado por:** Antigravity
- **Bloque:** B-10 (Gobernanza: 🅲 — Decisión de identidad institucional aprobada por Claude en `docs/auditorias/B-10.md`)
- **Fecha:** 2026-09-18
- **Estimado:** 40 min · **Real:** 35 min · **Desvío:** -12.5%
- **Commit:** `90573f5` — `data: resuelve SICSANTACRUZ tras la expiración de su dominio`
- **Estado:** `LISTO PARA AUDITORÍA`

---

## 1. Qué se ejecutó

Siguiendo la decisión de Claude en `docs/auditorias/B-10.md` (Opción 1 con encuadre preciso):

1. **Resolución en `output/excel_urls_diagnostic.json`**:
   - `Final_Url`: `"https://ice.santacruz.gob.bo/repositorio"`
   - `HTTP_Status`: `200`
   - `matched_keywords`: `["sic santa cruz", "precios", "agropecuario"]`
   - `mapping_note`: contiene estrictamente los cuatro puntos exigidos:
     - (a) El portal propio `sicsantacruz.com` expiró y hoy es parking comercial de Namecheap.
     - (b) El dato se publica a través del Instituto Cruceño de Estadística (ICE, `ice.santacruz.gob.bo/repositorio`), con el SIC nombrado como origen en la metodología de los estudios 13, 105 y 144, verificado mediante descarga real de PDFs y XLSX primarios (%PDF-1.4 y PK zip).
     - (c) **Es un canal sucesor, no la misma entidad**: si el ICE se incorpora alguna vez como fuente propia, hay que deduplicar contra esta fila.
     - (d) Para la etapa C, usar la API `/api/estudios`, no el HTML de la SPA.

2. **Registro de evidencia en `output/url_resolution_log.json`**:
   - Registro de auditoría actualizado con la evidencia completa:
     - Descarte de `sicsantacruz.com` (parking comercial con script de broker y meta referrer origin).
     - Descarte de subdominios `sic`, `sedacruz`, `agro`, `desarrolloproductivo` por error de resolución DNS (`getaddrinfo failed`).
     - Descarte de `sica.santacruz.gob.bo` por corresponder al Sistema de Inocuidad (sanidad animal, sin datos de precios de mercado).
     - Validación de `https://ice.santacruz.gob.bo/repositorio` como canal sucesor por contenido (D-01) con comprobación de magic bytes en descargas.

3. **Recálculo de cobertura del catálogo (68 fuentes)**:
   - Fuentes alcanzables por HTTP simple (HTTP 200): **63/68 = 92.6%**
   - **Fuentes verificadas y accesibles** (incluye BCP y BCRP vía headless): **65/68 = 95.6%**
   - Fuentes pendientes reales reducidas a 3:
     1. `FMI` (bloqueo Akamai 403)
     2. `FUNDEMPRESA` (410, concesión estatal concluida y transferida a SEPREC)
     3. `BOLCEREALES` (disuelta)

---

## 2. Evidencia literal y reproducible

### Prueba 1: Verificación de descargas de documentos con magic bytes
```
https://ice.santacruz.gob.bo/uploads/MONITOREO_DE_PRECIOS_AL_POR_MENOR_2024_v3_fedf660005.pdf -> 200
  Content-Length: 18452275
  Content-Type: application/pdf
  Magic bytes: b'%PDF-1.4\n%'
https://ice.santacruz.gob.bo/uploads/BBDD_IPC_PORMENOR_2023_685d4b9341.xlsx -> 200
  Content-Length: 2666462
  Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
  Magic bytes: b'PK\x03\x04\x14\x00\x06\x00\x08\x00'
https://ice.santacruz.gob.bo/uploads/ICE_Agropecuario_v1_ddc8250511.pdf -> 200
  Content-Length: 7932224
  Content-Type: application/pdf
  Magic bytes: b'%PDF-1.4\n%'
```

### Prueba 2: Recálculo de cobertura verificado
```bash
python -c "
import json
data = json.load(open('output/excel_urls_diagnostic.json', encoding='utf-8'))
total = len(data)
http_200 = sum(1 for r in data if str(r.get('HTTP_Status')) == '200')
headless = {'BCP', 'BCRP'}
accessible = sum(1 for r in data if str(r.get('HTTP_Status')) == '200' or r.get('Fuente') in headless)
print(f'Total sources: {total}')
print(f'HTTP 200 simple: {http_200}/{total} = {http_200/total*100:.1f}%')
print(f'Verificada y accesible: {accessible}/{total} = {accessible/total*100:.1f}%')
pending = [r['Fuente'] for r in data if str(r.get('HTTP_Status')) != '200' and r.get('Fuente') not in headless]
print('Pendientes:', pending)
"
```
Salida literal:
```
Total sources: 68
HTTP 200 simple: 63/68 = 92.6%
Verificada y accesible: 65/68 = 95.6%
Pendientes: ['BOLCEREALES', 'FMI', 'FUNDEMPRESA']
```

### Prueba 3: Verificación contra HEAD limpio en worktree independiente
```powershell
$ git worktree add --detach "$env:TEMP\wt_b10" HEAD
Preparing worktree (detached HEAD 90573f5)
HEAD is now at 90573f5 data: resuelve SICSANTACRUZ tras la expiración de su dominio

$ $env:PYTHONPATH = "$env:TEMP\wt_b10\src"; python -m pytest "$env:TEMP\wt_b10\tests" -q -m "not live"
......................................................                   [100%]
54 passed in 10.67s

$ $env:PYTHONPATH = "$env:TEMP\wt_b10\src"; python -m pytest "$env:TEMP\wt_b10\tests" -m "live" --collect-only -q
no tests collected (54 deselected) in 1.09s

$ git worktree remove "$env:TEMP\wt_b10"
```

---

## 3. Desviaciones del plan

Ninguna. Se aplicó estrictamente la Opción 1 con el encuadre (a, b, c, d) y sin incorporar al ICE como fuente independiente, tal como instruyó Claude.

---

## 4. Estado

`LISTO PARA AUDITORÍA`
