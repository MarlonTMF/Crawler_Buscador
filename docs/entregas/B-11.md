# B-11 · Cierre de Track A: Catálogo limpio y cobertura final recalculada

- **Ejecutado por:** Antigravity
- **Bloque:** B-11 (Cierre de Etapa B — Gobernanza: 🅰️→🅲, Antigravity recalcula y limpia; Claude valida la cifra final de Track A)
- **Fecha:** 2026-09-18
- **Estimado:** 25 min · **Real:** 15 min · **Desvío:** -40%
- **Commit:** `1684994` — `data: cierra Track A — catálogo sin duplicados y cobertura recalculada`
- **Estado:** `LISTO PARA AUDITORÍA`

---

## 1. Qué se hizo

1. **Depuración del registro basura `manual` duplicado**:
   - Se localizó el registro al final de `output/excel_urls_diagnostic.json` (`Fuente: "manual"`, `Url_Original: "https://www.bbcp.com.bo/"`, `Final_Url: "https://bbcp.org"`, `snippet_text: "<script>window.location.href=\"/lander\"</script>"`).
   - Dicho registro provenía de un ensayo manual previo intentando resolver una variante para `BOLCEREALES`. `bbcp.org` no es institucional sino una página genérica de aterrizaje (`/lander`) que devolvía HTTP 200 ficticio.
   - El registro fue eliminado del catálogo, eliminando la duplicación de la institución `BOLCEREALES`.

2. **Ajuste del registro canónico `BOLCEREALES`**:
   - Se fijó `Final_Url: null` (evitando apuntar a la variante inoperante `bbcp.com.bo`).
   - Se mantiene el estatus `HTTP_Status: "DISUELTA"`.
   - Se actualizó `mapping_note` documentando la fusión y descarte definitivo del lander no institucional.

3. **Cálculo exacto de cobertura sobre el catálogo limpio (67 fuentes)**:
   - El catálogo operativo queda consolidado en exactamente **67 fuentes**.
   - **HTTP 200 simple**: **62 / 67 = 92.54% (92.5%)**
   - **Verificadas y accesibles (métrica principal de Track A)**: **64 / 67 = 95.52% (95.5%)**
     *(62 fuentes en HTTP 200 + BCP en Cloudflare + BCRP en WAF accesibles mediante Headless)*
   - **Exclusiones documentadas y justificadas**: **3 / 67 = 4.48% (4.5%)**:
     1. `BOLCEREALES`: Institución disuelta sin sucesor único en la web.
     2. `FUNDEMPRESA`: HTTP 410 (concesión mercantil concluida y transferida por ley a SEPREC).
     3. `FMI`: HTTP 403 (bloqueo WAF Akamai EdgeSuite verificado que no cede con headless; eLibrary descarga 202 vacío).
   - **Total universo Track A auditado y cerrado**: **67 / 67 = 100.0%**.

4. **Sincronización de documentación**:
   - Se actualizó la tabla de Track A y el estado de la Fase 0 en `docs/plan_cobertura_100.md`, marcando la Fase 0 como completada y desbloqueando el arranque de la Etapa C (onboarding de Track B).

---

## 2. Evidencia literal y reproducible

### Prueba 1: Cálculo exacto de estados sobre el dataset maestro limpio
```bash
python -c "
import json
data = json.load(open('output/excel_urls_diagnostic.json', encoding='utf-8'))
total = len(data)
http_200 = sum(1 for r in data if str(r.get('HTTP_Status')) == '200')
headless = {'BCP', 'BCRP'}
accessible = sum(1 for r in data if str(r.get('HTTP_Status')) == '200' or r.get('Fuente') in headless)

print(f'Total fuentes: {total}')
print(f'HTTP 200 simple: {http_200}/{total} = {http_200/total*100:.2f}%')
print(f'Verificada y accesible: {accessible}/{total} = {accessible/total*100:.2f}%')

pending = [r['Fuente'] for r in data if str(r.get('HTTP_Status')) != '200' and r.get('Fuente') not in headless]
print('Exclusiones documentadas:', pending)

from collections import Counter
status_counts = Counter(str(r.get('HTTP_Status')) for r in data)
print('Desglose de estados:', status_counts)
"
```
Salida literal:
```
Total fuentes: 67
HTTP 200 simple: 62/67 = 92.54%
Verificada y accesible: 64/67 = 95.52%
Exclusiones documentadas: ['BOLCEREALES', 'FMI', 'FUNDEMPRESA']
Desglose de estados: Counter({'200': 62, '403_CLOUDFLARE_BLOCKED': 1, 'BOT_CHALLENGE': 1, 'DISUELTA': 1, '403_BOT_BLOCKED': 1, '410': 1})
```

### Prueba 2: Verificación contra HEAD limpio en worktree independiente
```powershell
$ git worktree add --detach "$env:TEMP\wt_b11" HEAD
Preparing worktree (detached HEAD 1684994)
HEAD is now at 1684994 data: cierra Track A — catálogo sin duplicados y cobertura recalculada

$ $env:PYTHONPATH = "$env:TEMP\wt_b11\src"; python -m pytest "$env:TEMP\wt_b11\tests" -q -m "not live"
......................................................                   [100%]
54 passed in 8.37s

$ $env:PYTHONPATH = "$env:TEMP\wt_b11\src"; python -m pytest "$env:TEMP\wt_b11\tests" -m "live" --collect-only -q
no tests collected (54 deselected) in 0.58s

$ git worktree remove "$env:TEMP\wt_b11"
```

---

## 3. Desviaciones del plan

Ninguna. Se depuró el duplicado de BOLCEREALES (`Fuente: manual`), se recalculó la métrica final sin estimaciones y se actualizó `docs/plan_cobertura_100.md`.

---

## 4. Estado

`LISTO PARA AUDITORÍA DE CLAUDE`
