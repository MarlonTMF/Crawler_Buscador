# B-04 · Resoluciones de URL verificadas

- **Ejecutado por:** Antigravity
- **Fecha:** 2026-09-18
- **Estimado:** 20 min · **Real:** 6 min · **Desvío:** -70%
- **Commit:** `71cba3f` — data: aplica las resoluciones verificadas por contenido de 6 URLs

## Qué se hizo

Se versionaron en `config/moved_urls.json` las 6 resoluciones de URL verificadas por contenido institucional (CADEXCO, IBCH, Aduana Nacional y 3 documentos de BCP). Se validó la carga sintáctica limpia del JSON y el cumplimiento estricto de D-01 (cada entrada contiene `reason` y `matched_keywords`).

## Criterio de aceptación — evidencia

> El JSON carga sin error (ya rompió una vez por una coma colgante), y cada entrada nueva tiene `reason` y `matched_keywords` — sin esos dos campos la resolución no cumple D-01 y no se commitea.

### 1. Validación de carga sintáctica JSON

```
$ python -c "import json; json.load(open('config/moved_urls.json', encoding='utf-8'))"
(salida vacía, código de salida 0)
```

### 2. Verificación de `reason` en cada entrada nueva

```
$ python -c "import json; m = json.load(open('config/moved_urls.json', encoding='utf-8')); faltan = [e['original'] for e in m[1:] if not e.get('reason')]; print('sin razon documentada:', faltan or 'ninguna')"
sin razon documentada: ninguna
```

### 3. Verificación de `matched_keywords` en cada entrada nueva

```
$ python -c "import json; m = json.load(open('config/moved_urls.json', encoding='utf-8')); faltan_kw = [e['original'] for e in m[1:] if not e.get('matched_keywords')]; print('sin matched_keywords:', faltan_kw or 'ninguna')"
sin matched_keywords: ninguna
```

### 4. Verificación aislada en checkout limpio de HEAD (worktree + PYTHONPATH)

```
$ git worktree add --detach "$env:TEMP\wt_head_b04" HEAD
Preparing worktree (detached HEAD 71cba3f)
HEAD is now at 71cba3f data: aplica las resoluciones verificadas por contenido de 6 URLs

$ $env:PYTHONPATH = "C:\Users\ELITEBOOK\AppData\Local\Temp\wt_head_b04\src"; python -m pytest tests/ -q -m "not live"
................................                                         [100%]
32 passed, 2 deselected in 10.06s

$ git worktree remove --force "$env:TEMP\wt_head_b04"
```

### 5. Inspección del commit

```
$ git show --stat 71cba3f
commit 71cba3fb6df249159b324fd78cbc8260060eb592
Author: MarlonTMF <marlontomasmarzofernandez@gmail.com>
Date:   Fri Sep 18 08:10:25 2026 -0400

    data: aplica las resoluciones verificadas por contenido de 6 URLs
    
    Todas verificadas contra el contenido de la página, no solo por status 200 (D-01), después de que tres sugerencias de IA apuntaran a instituciones distintas en sesiones anteriores.
    
    - CADEXCO -> cadexco.bo (verificado: 'Cochabamba', no CADEX Santa Cruz)
    
    - IBCH -> ibch.com (verificado: 'cemento', no IBCE comercio exterior)
    
    - BCP: 3 documentos remapeados a la nueva estructura /web/institucional/
    
    - Aduana Nacional: boletines movidos a /com_boletines
    
    Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

 config/moved_urls.json | 68 ++++++++++++++++++++++++++++++++++++++++++++++++++
 1 file changed, 68 insertions(+)
```

## Desviaciones del plan

Ninguna. Se aplicaron exactamente las 6 resoluciones con verificación completa.

## Dudas / escalamientos

En el working tree de `config/moved_urls.json` se encontraban 4 entradas de sesiones previas que no cumplen con los requisitos de D-01:
1. `https://www.sabsa.aero`: carece de campo `matched_keywords`.
2. `https://www.ceprobol.gob.bo`: carece de campo `matched_keywords`.
3. `https://www.vipfe.gob.bo`: SPA en Angular; su razón declara explícitamente que no se pudo verificar por HTML estático y requiere validación con navegador.
4. `https://www.fam.bo`: la razón argumenta migración a `https://fam.gob.bo/` mientras el campo resolved apunta a `https://fam.org.bo/`.

Conforme a la regla de que ninguna entrada sin verificación de contenido se commitea, estas 4 entradas fueron excluidas del commit y resguardadas en `output/moved_urls_pendientes_etapa_b.json` para ser clasificadas y resueltas durante la Etapa B.

## Estado

`LISTO PARA AUDITORÍA`
